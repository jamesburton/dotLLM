---
context: post-merge / sequential exploration
phase: null
task: refreshed after 2026-05-18 session
status: paused_clean
last_updated: 2026-05-18
branch: feature/qwen3.6
head: 7c7b443
---

<current_state>

**Branch `feature/qwen3.6 @ 7c7b443` — pushed to origin, working tree clean.** Test suite green on Strix Halo + this dev host (CPU + Vulkan).

This session: closed items 1, 2, 8 from the sequential-exploration plan; produced research artifacts + a code-ready probe for item 3; surfaced a third path (B3) for the LoRA Q8_0 −21% gap; posted the upstream dotnet/runtime#86849 comment to unstick the AVX-512 VNNI spec; sharpened the Phase 5a/5e (ForwardBatch) implementation plan to ready-to-paste code.

</current_state>

<completed_this_session>

7 commits on `feature/qwen3.6`:

- `8e8c228` — Attention: sliding-window mask off-by-one across 5 GPU kernels
  (3 Vulkan + 2 CUDA). New discriminating parity tests; full Vulkan suite
  559/2/0. CUDA PTX regen deferred to host follow-up.
- `48d65fe` — IQ3 dispatch in VulkanQwen3MoeHybridTransformerModel.
- `146d747` — IQ3 dispatch in VulkanNemotronHTransformerModel.
- `ad6b853` — IQ3 dispatch in VulkanMamba3TransformerModel (final host;
  IQ3_S + IQ3_XXS now wired across all 4 dense / MoE / NemotronH /
  Mamba3 transformer hosts).
- `3fdf117` — Notes: AVX-512 VNNI (VPDPBUSD-zmm) status + PR scope at
  `.planning/notes/dotnet-avx512-vnni-{status,pr-scope}.md`. Posted the
  unstick-the-spec comment to dotnet/runtime#86849 (see
  https://github.com/dotnet/runtime/issues/86849#issuecomment-4472274791).
- `3596528` → `7c7b443` — ForwardBatch implementation plan, sharpened to
  ready-to-paste code for the CPU refactor + override + Vulkan sketch +
  5 parity tests at `.planning/notes/forward-batch-impl-plan.md`.
- `dbf9b6a` — LoRA Q8_0 Stage 1 investigation report + probe extension.
  Background dotnet-perf-expert agent identified **Path B3** — repack
  LoRA-B at adapter-load into the existing R4 layout and route stage 1
  through MatMul.OuterProductGemmQ8_0 (4×6 AVX-512 microkernel, 24 ZMM
  accumulators). Zero new SIMD. Probe extension at
  `benchmarks/LoraQ8Stage1Probe/` adds the `Q8_0_R4_Reuse_OuterProduct`
  benchmark — user runs on Strix Halo to confirm B3 hits ≤0.47 ms at
  the canonical shape.

</completed_this_session>

<remaining_work>

Sequential-exploration plan — what's left in priority order:

1. **Strix Halo: run the LoRA Q8_0 probe** (`benchmarks/LoraQ8Stage1Probe/`).
   Shape sweep M∈{8,16,32,64} × K∈{2048,4096} × N∈{64,256,512}. Acceptance
   bar: B3 ≤0.47 ms at M=16/K=2048/N=512. **Gates item 3 productionisation.**

2. **Phase 5a + 5e — ForwardBatch CPU + Vulkan dense host implementation.**
   Plan + code-level design ready at `.planning/notes/forward-batch-impl-plan.md`.
   ~300 LoC implementation + ~250 LoC parity tests across two backends.
   1500-LoC hot-path refactor — needs a focused session with full test
   surface (F32/F16/Q8_0/K-quants × decode/prefill × KV/no-KV ×
   LoRA-active-per-seq).

3. **Item 3 productionisation (Path B3)** — once Strix Halo confirms the
   probe. Add `LoraWeightDType.Q8_0_R4` + `WeightRepacking.RepackR4_Q8_0`
   + dispatch arm in `LoraDelta.ApplyQ8_0BWithPreQuantX`. ~30 LoC. See
   `.planning/notes/lora-q8-stage1-investigation.md` §7.

4. **Item 6: Gemma 3 merge with Attention.Execute unification.** The
   Gemma3 commits `9bd0967` (Architecture enum + HF config) and
   `71ca9c3` (GeGLU activation kernel) are recoverable from reflog but
   **not on any branch** — `git worktree-agent-a56ac89c4decfb398` is
   gone. Recovery path: cherry-pick both onto current HEAD, then plan
   the Attention.Execute unification (ALiBi + softcap parameter
   overloads). User selected "audit commits first, then plan the
   merge" for the recovery approach. Audit deferred to next session.

5. **Item 4: Strix Halo measurement triad** (FA pp512 + Hybrid + Qwen3.6-A3B
   baseline). Pre-wired benches; user runs on Strix Halo.

6. **Phase 5b + 5f — full matmul fusion in batched path** (deferred from
   item 5 plan). GQA-non-LoRA-non-MoE, ~550 LoC. Follow-up after 5a/5e.

7. **Item 7: Phase 9 Step 59 advanced scheduling** (PD-disag + priority
   preemption + fairness + chunked-prefill tuning). Multi-week.

8. **dotnet/runtime PR draft** (item 8 continuation). Waiting on
   @tannergooding's response to https://github.com/dotnet/runtime/issues/86849#issuecomment-4472274791
   before sinking PR effort.

</remaining_work>

<blockers>

- **dotnet/runtime#86849 spec re-confirmation** — comment posted
  2026-05-17 (issuecomment-4472274791); waiting on @tannergooding /
  @saucecontrol response. Blocks the cleanest path for closing the
  −16% LoRA Q8_0 prefill gap, but **Path B3 (R4 reuse) bypasses this**
  with zero upstream dependency.
- **Strix Halo access** — needed for items 1, 3-prod, 4, 5b/5f bench
  validation. Code/plans all stage-able from a non-Strix host.
- **`worktree-agent-a56ac89c4decfb398` is gone** — Gemma 3 commits
  recoverable from reflog (9bd0967, 71ca9c3). Plan the merge before
  cherry-picking.

</blockers>

<context>

The session pattern: scope each task honestly, ship a quality artifact
(code or sharp plan), commit, move on. Items 1, 2, 8 closed cleanly;
items 3, 5, 6, 4, 7 staged at varying readiness levels.

The Path B3 finding for item 3 is the most important: it reduces the
−21% LoRA Q8_0 gap from "wait months for upstream VNNI" or "write a
new R16 kernel" to a one-shot R4 adapter-load repack that reuses an
already-validated production kernel. If the Strix Halo probe confirms,
the productionisation patch is ~30 LoC.

Item 5 (ForwardBatch) is plan-ready but implementation-deferred — the
Forward method on TransformerModel.cs is 670 lines of GQA/MLA/MoE/LoRA
branching, and a clean refactor with full parity coverage warrants a
dedicated session, not the tail of a multi-task session.

</context>

<next_action>

**On Strix Halo**: run `benchmarks/LoraQ8Stage1Probe/` to validate
Path B3. If green, opens 30-LoC productionisation; if red, fall back
to Path B2 (R16 dedicated kernel, also documented in §1.4 of the
investigation report).

**Off Strix Halo**: open a focused session for Phase 5a/5e
implementation following `.planning/notes/forward-batch-impl-plan.md`.
Or, smaller-scoped: audit + cherry-pick the Gemma 3 commits (item 6)
and plan the Attention.Execute unification.

</next_action>
