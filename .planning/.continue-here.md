---
context: post-merge / sequential exploration
phase: null
task: refreshed after 2026-05-18 session 2 (Strix Halo)
status: paused_clean
last_updated: 2026-05-18
branch: feature/qwen3.6
head: 64a561c
---

<current_state>

**Branch `feature/qwen3.6 @ 64a561c` — pushed to origin, working tree clean.** Full test suite green on Strix Halo (CPU + Vulkan).

This session ran on the actual Strix Halo dev host (confirmed: AMD Ryzen AI Max+ 395, 16C/32T, Radeon 8060S iGPU). All benches / probes / tests ran on production-target hardware directly, no dev-laptop intermediary.

</current_state>

<completed_this_session>

13 commits on `feature/qwen3.6` across two session-halves:

**Session 1 (off-Strix-Halo, planning + research):**
- `8e8c228` — Attention sliding-window mask off-by-one across 5 GPU kernels (Vulkan x3 + CUDA x2)
- `48d65fe` / `146d747` / `ad6b853` — IQ3 dispatch in remaining 3 Vulkan hosts (Qwen3MoeHybrid / NemotronH / Mamba3)
- `3fdf117` — AVX-512 VNNI research notes + posted https://github.com/dotnet/runtime/issues/86849#issuecomment-4472274791
- `3596528` → `7c7b443` — ForwardBatch implementation plan + ready-to-paste code
- `dbf9b6a` — LoRA Q8_0 Stage 1 background investigation + probe extension (Path B3)
- `4073eea` — refreshed continue-here

**Session 2 (Strix Halo execution):**
- `dd2892f` — **Path B3 (R4 reuse for LoRA Q8_0 Stage 1) DISCONFIRMED.** Probe on Strix Halo measured ~840 us at canonical shape vs ≤470 us acceptance bar. Same magnitude as the currently-shipped `Q8_0_GemmPreQuantX` (~770 us). Productionisation cancelled.
- `380e4b9` — **Vulkan FA microbench captured in PERFORMANCE.md §6.4.** pp512 1.35× / pp2048 2.06× / pp4096 2.72× FA-over-naive scaling on Strix Halo. llama.cpp Vulkan FA comparison still outstanding (need llama.cpp build).
- `479c23f` — **ForwardBatch CPU Phase 5a shipped + parity-tested.** Refactor extracts `RunLayersAndFinalNormCore` + `RunLmHead` from `Forward`; `ForwardBatch` snapshots per-seq hidden states to an ArrayPool buffer + runs one batched lm_head GEMM. 4 parity tests bit-equal on Strix Halo (SmolLM-135M Q8_0); 109/109 architecture regression tests pass.
- `9895ca3` — Phase 5e (Vulkan dense host ForwardBatch) deferred. Finding: Vulkan's `Forward` only runs lm_head on the last token (seqLen=1, returns [1, vocab]), so the lm_head-fusion saving is ~150-350 µs per batch step — noise floor. The real Vulkan win waits for Phase 5f (intra-block matmul fusion).
- `64a561c` — Gemma 3 merge **audited and deferred**. Cherry-pick of `9bd0967` hit a silent text-merge corruption (SmolLM3 NoPE tests added since the commit was authored; patch context `}→}` matched at the wrong location). Manual merge plan documented at `.planning/notes/gemma3-merge-audit.md`. Prerequisite commits pinned via tags `gemma3-step57-config-pending` (9bd0967) and `gemma3-step57-geglu-pending` (71ca9c3), both pushed to origin.

</completed_this_session>

<remaining_work>

Priority order, all blocking constraints removed unless noted:

1. **Phase 5b/5f — intra-block matmul fusion in batched ForwardBatch.**
   The lm_head-only Phase 5a CPU win is shipped. The big win is fusing
   Q/K/V/O/gate/up/down matmuls across sequences. See plan §"Phase 5b"
   and §"Phase 5f" at `.planning/notes/forward-batch-impl-plan.md`.
   Estimated 1-2 sessions of focused work each (CPU + Vulkan).

2. **`HybridPrefillDecodeBenchmarks` BDN discovery skip** (task #13).
   Interactive BDN listing shows benchmark IDs #15, #19, #21 are silently
   filtered — Hybrid + 2 others. All compile cleanly; all have correct
   attributes. Hypothesis: nested enum `[Params(BenchmarkMode...)]` or
   `InferenceResponse` return type confuses BDN reflection. Needs
   isolation repro. **Blocks the H4 first-32-tokens win acceptance check.**

3. **llama.cpp Vulkan FA benchmark on Strix Halo** — needed to close
   GAIA H2's "≥60% of llama.cpp FA pp512 t/s" acceptance metric. Gating
   on a llama.cpp build configured for this host (`-DGGML_VULKAN=ON`).
   See PERFORMANCE.md §6.4 outstanding measurements list.

4. **Qwen3.6-A3B GGUF download for the perf triad's third leg.** Only an
   Ollama blob of the 3.5 series is cached here. Not a hard blocker;
   HANDOFF.json baseline-qwen36-a3b-cpu.json remains the reference.

5. **CUDA PTX regeneration** — `attention.cu` and `attention_f32.cu`
   were patched for the sliding-window off-by-one (`8e8c228`); PTX is
   stale on Strix Halo (no nvcc). Re-generate next time a CUDA host
   is available.

6. **Item 6 Gemma 3 substantive work** — Attention.Execute soft-cap
   parameter integration + per-layer attention type dispatch +
   final-logit cap + end-to-end forward. Prerequisite commits pinned
   via tags; manual merge plan at `.planning/notes/gemma3-merge-audit.md`.
   1-2 sessions when prioritised.

7. **Item 7 Phase 9 Step 59 advanced scheduling** — PD-disag + priority
   preemption + fairness + chunked-prefill tuning. Multi-week.

8. **dotnet/runtime PR draft** — gated on @tannergooding response to
   https://github.com/dotnet/runtime/issues/86849#issuecomment-4472274791.
   Scope drafted at `.planning/notes/dotnet-runtime-avx512-vnni-pr-scope.md`.

</remaining_work>

<blockers>

- **dotnet/runtime#86849 response** — affects only the upstream PR path.
  Path B3 (would have been the workaround) is disconfirmed; no
  in-tree fix path for the −16% LoRA Q8_0 prefill regression remains
  open. Either wait for upstream VNNI or accept the gap.
- **CUDA host for PTX regeneration** — pending.
- **llama.cpp Vulkan build on Strix Halo** — pending.

</blockers>

<context>

This session converted ~6 hours of planning + research into shipped
code + verified perf measurements + a definitive negative result that
saved further investigation effort:

- **Path B3 negative result** prevented spending another 1-2 sessions
  productionising a kernel that wouldn't have closed the LoRA Q8_0
  gap. The investigation was correct in form (mechanism analysis,
  acceptance bar) but wrong in prediction; the structural argument
  about Zen 5 prefetcher streams didn't hold empirically at M=16.
- **ForwardBatch CPU Phase 5a** is the first concrete throughput-win
  for the scheduler's continuous-batching path. Byte-identical parity
  + 109/109 regression-clean.
- **Strix Halo FA bench** validated that the FA shader scales 1.35→2.72×
  with sequence length on this hardware — the BR=BC K-amortisation
  prediction holds.

The Gemma 3 cherry-pick failure is a useful teaching moment for any
future "revive a dropped commit from reflog" workflow: git's text-merge
silently corrupts when patch context drifts in adjacent additive areas.
The audit + tag pattern is the safe recovery path.

</context>

<next_action>

Two equally valuable next-session candidates:

- **Phase 5b — CPU intra-block matmul fusion in ForwardBatch.** Bigger
  win than Phase 5a (lm_head-only). Plan ready at
  `.planning/notes/forward-batch-impl-plan.md` Phase 5b section.
  ~250 LoC + ~150 LoC tests, ~half-day focused work. Same parity-test
  pattern as Phase 5a (use SmolLM-135M Q8_0 fixture).

- **BDN Hybrid discovery skip** — isolation repro on a fresh minimal
  bench project. If the cause is a known BDN limitation, file a
  GitHub issue + apply the workaround in the repo. Unblocks the H4
  acceptance check.

</next_action>
