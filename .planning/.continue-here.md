---
context: post-merge / sequential exploration
phase: null
task: refreshed after 2026-05-18 session 3 (parallel worktree merge + audit follow-ups)
status: paused_clean
last_updated: 2026-05-18
branch: feature/qwen3.6
head: 8ea54ea
---

<current_state>

**Branch `feature/qwen3.6 @ 8ea54ea` — pushed to origin, working tree clean.** 743/743 tests green on Strix Halo. Repo root cleaned (14 obsolete .continue-here-*.md files removed; HANDOFF.json removed; .planning/.continue-here.md is the single live handoff).

</current_state>

<completed_this_session>

**Session 3 (parallel worktree run + merges + audit follow-ups), 8 new commits:**

- `e293cba` BDN discovery skip: drop `sealed` on `HybridPrefillDecodeBenchmarks` (BDN's Reflection.Emit per-case subclassing rejects sealed types — root caused by parallel-agent investigation).
- `696613f` + `dbef246` Gemma 3 prerequisite revival from reflog: Architecture enum + HF config + GeGLU activation kernel (manual splice on the config file, clean cherry-pick on the GeGLU file). 22/22 + 44/44 tests pass.
- `92c1345` ForwardBatch CPU intra-block matmul fusion (Phase 5b) — Q/K/V/O/gate/up/down all run as single batched GEMMs across the simple subgroup (GQA / non-MLA / non-MoE / non-LoRA). Per-seq attention. **2.09× speedup** on Strix Halo SmolLM-135M Q8_0 at 4× decode. Complex sequences fall back to per-seq.
- `1c04887` ForwardBatch Vulkan intra-block matmul fusion (Phase 5f, dense host) — same pattern via `VulkanForwardBatchScratch` + slice-copy attention through per-seq scratch buffers. 4 new parity tests pass, full Vulkan suite 563/0/2.
- `152fa16` State audit doc (501 lines) at `.planning/notes/2026-05-18-state-audit.md` — written by the parallel auditor, identified C1/C2/H1-6/S1-4 follow-ups.
- `5540e24` / `3983ec6` / `82f8af6` / `33e7e20` Four `--no-ff` merge commits, one per worktree branch.
- `8ea54ea` Housekeeping batch addressing C2 + H1 + H2 + H5 + S1-S4 (test attribute fix, ROADMAP/README/SUPPORTED_MODELS docs reconciliation, PERFORMANCE.md header renumber, 14 stale .continue-here-*.md deletions, HANDOFF.json deletion, frontmatter status updates, SCHEDULING.md Phase 5x narrative).

Five parallel agents in worktree isolation; all four implementation branches merged cleanly with zero file conflicts. Each agent's worktree was removed after the merge.

</completed_this_session>

<remaining_work>

Priority order, hardware constraints noted:

1. **C1 audit — CUDA F16 attention sliding-window discriminator test** (BLOCKED on CUDA host).
   `8e8c228` patched `native/kernels/attention.cu` (F16) + `attention_f32.cu` for the sliding-window off-by-one, but the CUDA tests only exercise `slidingWindow=0`. Same shape as the GDN head-broadcast bug — high-priority once a CUDA host is available. Mirror
   `VulkanAttentionF32KernelTests.Launch_SlidingWindow_DiscriminatesOffByOne` on the CUDA side; also add a new `AttentionF16ParityTests` (none currently exists). See audit §C1.

2. **H6 — CUDA PTX regen** (BLOCKED on nvcc host). The patched `.cu` files
   haven't been recompiled to PTX since `8e8c228`; CUDA models using
   sliding window currently inherit the buggy mask via the stale PTX.

3. **H3 — IQ3 per-host real-GGUF parity tests** (~3-4h). The 16 IQ3
   kernel-level parity tests don't exercise the dispatch arms; a miswired
   case label / codebook handle / IQ2-vs-IQ3 typo would pass kernel tests
   and silently mis-decode at inference. Needs a tiny synthetic IQ3 GGUF
   fixture or inline GGUF-writer + IQ3 quantiser. See audit §H3.

4. **H4 — ForwardBatch LoRA / MLA / MoE / quantized-KV parity test
   breadth** (~half-day). Phase 5a/5b/5f all claim per-seq fallback for
   complex sequences works correctly, but no tests exercise: ForwardBatch
   with two seqs using *different* LoRA adapters, ForwardBatch on MLA
   (DeepSeek-V2-Lite), ForwardBatch on MoE, ForwardBatch with paged +
   quantized KV-cache. See audit §H4.

5. **Phase 5b/5f follow-up — fuse other 3 Vulkan transformer hosts**
   (~half-day per host). Phase 5f shipped only the dense host. Mirror
   onto `VulkanQwen3MoeHybridTransformerModel`, `VulkanNemotronHTransformerModel`,
   `VulkanMamba3TransformerModel`. Same pattern as the dense-host commit
   `1c04887`.

6. **llama.cpp Vulkan FA bench on Strix Halo** for the GAIA H2 acceptance
   metric. Requires a llama.cpp build with `-DGGML_VULKAN=ON` against the
   same Vulkan SDK. Setup work, not blocked.

7. **Item 7 — Phase 9 Step 59 advanced scheduling** (multi-week scope):
   PD-disag + priority preemption + fairness + chunked-prefill tuning.

8. **dotnet/runtime PR draft** — passive: waiting on @tannergooding
   response to https://github.com/dotnet/runtime/issues/86849#issuecomment-4472274791.
   Path B3 negative result means there is no in-tree fallback open for
   the LoRA Q8_0 −16% prefill gap.

9. **Gemma 3 substantive work** (~1-2 sessions). Attention.Execute
   soft-cap parameter integration + per-layer attention type dispatch +
   final-logit cap + end-to-end forward. Prerequisites now in tree
   (commits `696613f` + `dbef246`); plan recorded at
   `.planning/notes/gemma3-merge-audit.md`.

</remaining_work>

<blockers>

- CUDA host needed for: C1 (sliding-window F16 test), H6 (PTX regen).
- llama.cpp Vulkan build needed for: GAIA H2 acceptance comparison.
- Everything else is non-blocked.

</blockers>

<context>

Session 3 was the parallel-worktree session: 5 agents in isolated
worktrees, 4 merged back to feature/qwen3.6, full housekeeping pass on
top. The pattern worked cleanly — no file conflicts across the 4
implementation branches because the file sets were truly disjoint
(CPU TransformerModel vs Vulkan VulkanTransformerModel vs benchmarks vs
core config + kernels).

Total session output: 4 substantive shipped features (Phase 5b CPU
matmul fusion, Phase 5f Vulkan matmul fusion, BDN discovery fix, Gemma 3
prerequisite revival) + 1 audit doc + 1 housekeeping batch. The Phase
5b CPU win (2.09× at 4× decode) is real and validated.

The audit doc itself (`.planning/notes/2026-05-18-state-audit.md`) is
the most useful reference for what's left — concrete file paths and
line numbers for each gap.

</context>

<next_action>

For the next session, in priority order:

- **If on a CUDA host:** C1 + H6. Mirror the Vulkan FA sliding-window
  test on CUDA F32 + add F16 parity, regen PTX. ~half-day.
- **Otherwise:** H4 (ForwardBatch parity test breadth) — the test
  coverage gap that's most likely to surface a real bug. Or H3 (IQ3
  per-host real-GGUF tests) — cross-backend safety.
- **Bigger lift:** Phase 5b/5f follow-up on the 3 non-dense Vulkan hosts
  (Qwen3MoeHybrid, NemotronH, Mamba3). Same 450-LoC pattern × 3.
- **Substantive Gemma 3 work** if there's a real Gemma 3 use case.

</next_action>
