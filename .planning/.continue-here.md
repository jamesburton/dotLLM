---
context: post-merge / sequential exploration
phase: null
task: 1
total_tasks: 8
status: paused_clean
last_updated: 2026-05-16T14:05:24.253Z
branch: feature/qwen3.6
head: 07f391f
---

<current_state>

**Branch `feature/qwen3.6 @ 07f391f` — pushed to origin, working tree clean.** Test suite green on Strix Halo (CPU + Vulkan).

This session merged the 8 parallel follow-up agents' work and shipped Phase 4d.6 LoRA stage-2 outer-product win + IQ3 dense-host dispatch wiring. Three of four LoRA variants (F32, F16, BF16) now meet the ≤−10% prefill gate on Strix Halo; decode +7–18% across the board. Q8_0 LoRA still at −21% — root cause investigated separately (see `.continue-here-lora-final-mile.md`), needs either dotnet/runtime#86849 (512-bit VPDPBUSD) or R16-interleaved B repack to close.

</current_state>

<completed_work>

This session (~70 commits over the day, all on origin):

- **Phase 4d.6** — outer-product LoRA stage-2 fast path + lazy transposed-A cache (6.9× stage-2 speedup, end-to-end +3.3×). F32/F16/BF16 LoRA prefill regressions: −23% / −25% / −25% → −5% / −8% / −9% (all gate met). Decode +7–18%.
- **Tiny-M Q8_0 v2 investigation** — disciplined negative result. Path C (16 explicit Vector locals), Path B (R16 interleaved), Path F (fused) all measured slower than F32 baseline at LoRA-stage-1 shapes on Zen 5. Conclusion: 512-bit VPDPBUSD missing from .NET 10/11 is the bottleneck.
- **.NET 11 AVX-512 VNNI status research** — verdict `STATUS_API_APPROVED_NOT_IMPLEMENTED`. Infrastructure (PR #116230) landed; public API in design limbo waiting on `AvxVnni.V512` shape confirmation. PR scope drafted at `.planning/notes/dotnet-runtime-avx512-vnni-pr-scope.md`.
- **GDN multi-token Vulkan shader fix** — FMA-fusion root cause (not algorithmic); `precise float` qualifiers; 19/19 tests now green. Earlier 6 failures resolved.
- **Critical Q3_K cross-backend dequant bug fix** — sub_12..15 byte layout (CPU + CUDA + Vulkan, PTX regen pending on CUDA host).
- **Vulkan zero-copy GGUF framework** — `VK_EXT_external_memory_host` probed; amdvlk on Strix Halo rejects read-only mmap pointers, framework activates when CoW mmap workaround / Mesa radv / driver fix lands.
- **GAIA H2 Vulkan Flash Attention** — 2.49× pp512 on dev iGPU; Strix Halo headline bench wired but unrun.
- **GAIA H4 Hybrid CPU-prefill / iGPU-decode** — `HybridPrefillDecodeStrategy` + KV handoff + 4 integration tests passing; Strix bench wired but unrun.
- **Phase 9.37 advanced prompt caching** — PrefixTrie + refcount + LRU + server `/v1/prompt-cache/{id}` endpoints.
- **Phase 9.35 scheduler-gap closure** (independent parts): `IModel.ForwardBatch` API + telemetry providers wired + server `RunLoopAsync` background task + handoff docs.
- **IQ3_S + IQ3_XXS** — CPU oracle (DequantizeIQ3.cs + Iq3SGrid + Iq3XxsGrid codebooks) + Vulkan kernels via SSBO codebook pattern + 16 parity tests + dense-host dispatch wired.

</completed_work>

<remaining_work>

Sequential exploration plan (`Task #13`) — 7 items left in priority order:

1. **IQ3 dispatch in remaining 3 Vulkan hosts** (Qwen3MoeHybrid, NemotronH, Mamba3) — same 7-step recipe as dense host, see `.continue-here-iq3-dispatch.md`. ~30 min per host. Pure mechanical wins.
2. **`attention_f32.comp` sliding-window mask off-by-one** — FA agent flagged the legacy shader has `> window` should be `≥ window`. Single-line fix + SPV regen.
3. **Q8_0 LoRA Stage 1 residual investigation** — close the −21% gap. Either wait for dotnet/runtime#86849 (cleanest) or implement R16-interleaved B repack with new `LoraWeightDType.Q8_0_R16` (relax-anti-goal path).
4. **Strix Halo measurement triad** — run the pre-wired Vulkan FA pp512 bench + Hybrid Prefill/Decode 64-token bench + Qwen3.6-A3B baseline (if cached). All benches exist, just need execution + numbers into `docs/PERFORMANCE.md`.
5. **ForwardBatch backend overrides** — CPU + Vulkan implementations of `IModel.ForwardBatch` for kernel-level batched Forward across sequences (today's default is per-sequence loop). Documented in `.continue-here-scheduler-batched-forward.md`.
6. **Gemma 3 merge with Attention.Execute unification** — preserved on branch `worktree-agent-a56ac89c4decfb398` (now possibly removed; recheck). ALiBi + softcap parameter overloads need careful surgical merge.
7. **Phase 9 Step 59 advanced scheduling** — PD-disag + priority preemption + fairness + chunked-prefill tuning. Multi-week scope; depends on Scheduler MVP (merged).
8. **dotnet/runtime AVX-512 VNNI PR draft** — local fork prep against the existing `AvxVnniInt8.V512` template, ~14 files / ~250 hand-written lines. PR scope already drafted at `.planning/notes/dotnet-runtime-avx512-vnni-pr-scope.md`. First action: post a focused comment on dotnet/runtime#86849 asking @tannergooding to confirm `AvxVnni.V512` shape, THEN draft the diffs.

Deferred Gemma 3 follow-up tracked as Task #10.

</remaining_work>

<decisions_made>

- **Sequential, in main repo** (not parallel worktrees) — chosen by user when disk hit 0 GB free. Trade-off: slower wall-clock but no contamination risk.
- **Q3_K cross-backend bug fixed in this branch despite affecting CUDA** — Vulkan kernels just shipped match the new CPU oracle; CUDA `.cu` source fixed but PTX regen needs CUDA-host build. Marked clearly in commit message.
- **LoRA Q8_0 Phase 4d.5 plumbing stays gated behind `DOTLLM_LORA_FORCE_Q8_PREQUANT=1`** — Phase 4d.6's stage-2 fix did not change this. Q8_0 Stage 1 needs separate kernel work or the upstream API.
- **GDN single-token shader `precise` qualifiers NOT applied** — only multi-token shader was the production dispatch site. Documented as a consistency follow-up.
- **`worktree` isolation has proven unreliable** — multiple agents in batch #2 contaminated each other's git history. Recommend: avoid isolation mode for future agent batches, just commit directly to feature/qwen3.6 and integrate.

</decisions_made>

<blockers>

- **dotnet/runtime#86849** — 512-bit `Avx512Vnni.MultiplyWideningAndAdd` (VPDPBUSD-zmm) not exposed in .NET 10.0.103 or .NET 11 main. Blocks closing the −21% LoRA Q8_0 prefill gap via the cleanest path. Workaround: upstream PR (~14 files, well-templated). User said "I'm game" — needs an issue comment first to get @tannergooding's nod on API shape.
- **amdvlk on Strix Halo rejects read-only mmap pointers** for `VK_EXT_external_memory_host` import. Blocks zero-copy GGUF win. Workaround: secondary CoW mmap or Mesa radv on Linux.
- **TinyLlama Q8_0 GGUF not cached** on Strix Halo — Phase 4d.6 bench used Llama-3.2-1B as stand-in. Macro-bench acceptance verified anyway.

</blockers>

<context>

This session was the integration sprint after the parallel-agent batch landed. The big win is that LoRA F32/F16/BF16 are now production-ready (within 10% of NoLora baseline + decode-positive). Q8_0 LoRA is opt-in but durable.

The most interesting finding was reframing the LoRA perf problem: prior agents assumed stage-1 was the bottleneck (per-block Q8_0 GEMM); Phase 4d.6 agent measured everything and found stage-2 was 85% of wall time. One 6.9× speedup later, the gate moved.

For the next session: items 1, 2, and 4 from the remaining list are all <1hr each and would close visible loose ends. Item 8 (dotnet/runtime PR) is high-impact community contribution if the user wants to spend a focused day. Item 7 (Step 59) is the largest and needs its own session.

Worktree isolation issues observed this session: agents writing into the same logical git history view despite each having their own worktree branch. Avoid `isolation: "worktree"` going forward; the cherry-pick-by-SHA pattern was robust but expensive.

</context>

<next_action>

Run `/gsd:resume-work` to get the status board, then proceed through the sequential exploration plan starting at item 1 (IQ3 dispatch in remaining 3 Vulkan hosts). Each host follows the 7-step recipe documented in `.continue-here-iq3-dispatch.md` — fields, ctor params, init, factory, invalidate, dispatch arms, dispose. Build + run `dotnet test --filter "FullyQualifiedName~Iq3|FullyQualifiedName~Vulkan{HostName}TransformerModel"` after each host. Commit + push per host.

</next_action>
