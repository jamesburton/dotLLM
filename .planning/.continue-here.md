---
context: post-merge / sequential exploration
phase: null
task: refreshed after 2026-05-18 session 6 (audit-H3 fully closed)
status: paused_clean
last_updated: 2026-05-18
branch: feature/qwen3.6
head: 17f2072
---

<session_5_summary>

Round-3 spawned 4 parallel worktree agents; 3 of 4 shipped:

1. **IQ3 upload-path predicates** (3 commits, branch deleted) — closed audit-H3
   on Mamba3 + NemotronH; Qwen3MoeHybrid still gated on a separate
   `BuildFromPrebuiltWeights` factory. 12 previously-skipping IQ3 tests
   now pass on Strix Halo.
2. **Vulkan Gemma 3 wiring** (1 combined commit, branch deleted) — softCap
   + per-layer sliding window + QPAS + FinalLogitSoftcap propagated into
   `VulkanTransformerModel` + 4 shaders extended with `softCap` /
   `scaleOverride` push constants. 5 new Gemma3 Vulkan-vs-CPU parity
   tests pass at abs 5e-3 / rel 1e-3.
3. **Qwen3MoeHybrid per-seq GDN state** (3 commits, branch deleted) — new
   `IGdnState` interface + `SequenceForwardRequest.GdnState` slot. CPU /
   Vulkan / CUDA Qwen3MoeHybrid models all consume per-seq state. Multi-seq
   ForwardBatch guard lifted on Vulkan; ships per-seq dispatch (not full
   fusion — every layer in the host is per-token GDN scan or MoE routing).
4. **Mamba-3 per-seq state** — agent refused mid-task citing coordination
   concerns (Option A vs B choice + cross-backend implications). Queued
   for next session — Option B precedent is now set by IGdnState; mirror
   as `IMambaState` interface.

Branch HEAD now `0c01b5b`. All 3 successful worktree branches merged + deleted
from origin + local.

</session_5_summary>

<current_state>

**Branch `feature/qwen3.6 @ 5ca9271` — pushed to origin, working tree clean.** All worktree branches deleted from origin and local; no orphaned refs.

</current_state>

<session_6_summary>

Session 6 (sequential, single-agent), 3 commits:

1. **Qwen3MoeHybrid `BuildFromPrebuiltWeights` factory** (commit `5ca9271`) — last
   IQ3 upload-path prerequisite for audit-H3 closed. Mirror of
   `VulkanNemotronHTransformerModel.BuildFromPrebuiltWeights`; caller owns every
   unmanaged pointer including routed MoE expert banks.

2. **Continue-here refresh** (commit `195345c`) — pruned remaining-work list
   (items 2 + 5 had become stale).

3. **Qwen3MoeHybrid IQ3 fixture builder + parity tests** — replaced the 4
   placeholder SkippableFact stubs with a real
   `Qwen3MoeHybridIq3FixtureBuilder` synthesising a 2-layer GDN+full-attn model
   in unmanaged memory, IQ3-quantising every matmul-target projection
   (GDN QKV/Gate/Alpha/Beta/Out, full-attn Q/K/V/O, lm_head) via
   `Iq3Fixture`. All 4 tests now PASS on Strix Halo at abs 0.15 / rel 0.15.
   Audit-H3 trap-the-bug closure is full across the 4-host Vulkan matrix
   (dense / Mamba3 / NemotronH / Qwen3MoeHybrid).

</session_6_summary>

<completed_this_session>

**Session 4 (round-2 parallel worktree agents), 11 new commits**:

Round-2 agents ran 4 in parallel:

1. **Phase 5b/5f mirror on 3 non-dense Vulkan hosts** (worktree-agent-a1317dcc, 3 commits):
   - `fface45` NemotronH — full intra-block matmul fusion (SSM stays per-seq; GQA layers + lm_head fuse). 4 new parity tests pass.
   - `1f4f3f3` Mamba-3 — ForwardBatch override scaffolding + multi-seq guard (`NotSupportedException` because `VulkanMamba3State` is single-instance and would corrupt cross-seq state). Single-seq parity test ships.
   - `8e5d67f` Qwen3MoeHybrid — same multi-seq guard (GDN single-state container + per-layer MoE = no safe fusion target today). SkippableFact placeholders.
   - **Architectural finding**: per-sequence recurrent-state isolation is missing on Mamba-3 + Qwen3MoeHybrid. Same latent issue on CPU `Mamba3TransformerModel`. Tracked as follow-up.

2. **H4 ForwardBatch parity test breadth** (worktree-agent-ac362476, 1 commit):
   - `5e61c14` — 4 new parity tests covering: two seqs with disjoint LoRA adapters (bit-exact); MLA model (DeepSeek-V2-Lite trimmed); quantized-KV-cache (SmolLM-135M Q8_0); heterogeneous adapter-on-one-seq-only batch. All cross-backend invariants verified clean.

3. **Gemma 3 substantive work** (worktree-agent-af1bc0282..., 3 commits pushed directly to feature/qwen3.6):
   - `532afb2` CPU `Attention.Execute` softCap parameter (Gemma 2 `attn_logit_softcapping`).
   - `d6a7e6c` TransformerModel wiring: `PerLayerSlidingWindow` per-layer dispatch, `AttnLogitSoftcap` plumbing, `QueryPreAttnScalar` scale override, `FinalLogitSoftcap` at lm_head (both per-seq and batched paths). All four are null-no-op when ModelConfig field is null.
   - `666fae8` Synthetic Gemma 3 e2e forward test — 4 discriminative tests (all-mechanisms / softcap-bounds / per-layer-sliding-window / qpas).

4. **IQ3 per-host parity tests** (worktree-agent-af286c5f, 4 commits):
   - `0c793e6` Mamba3 — 6 SkippableFact tests.
   - `3ed761d` NemotronH — 6 SkippableFact tests.
   - `bb11d3a` Dense `VulkanTransformerModel` — 4 tests, **all PASS on Strix Halo**.
   - `a28fed1` Qwen3MoeHybrid — 4 SkippableFact placeholders.
   - **Audit-H3 trap-the-bug surfaced**: IQ3 dispatch arms wired into the 3 non-dense Vulkan hosts (commits `ad6b853`/`146d747`/`48d65fe`) are **unreachable in tree** because their upload-path `KeepQuantOnDevice` / `DeviceQuantTypeFor` predicates don't list IQ3_S / IQ3_XXS. Documented in `.planning/notes/iq3-per-host-parity-deferred.md`. Tests auto-promote from skip to passing once the predicates are added.

Plus 3 merge commits (`badd72c` / `7463761` / `8366eb7`) and origin/local worktree-branch cleanup.

</completed_this_session>

<remaining_work>

1. **Mamba-3 per-sequence state isolation** (non-blocked, ~half-day) —
   mirror the `IGdnState` pattern shipped this session as `IMambaState`:
   new interface in `src/DotLLM.Core/Models/IMambaState.cs`, slot on
   `SequenceForwardRequest`, CPU/Vulkan/CUDA Mamba3State classes implement
   it, drop the multi-seq guard at `VulkanMamba3TransformerModel.ForwardBatch`
   (commit `1f4f3f3` is the guard to remove), add multi-seq parity test.
   The Qwen3MoeHybrid commits (`03f7ab9` / `a3ad719` / `0f3e4ce`) are the
   reference implementation.

2. ~~**Qwen3MoeHybrid BuildFromPrebuiltWeights factory**~~ — SHIPPED in session 6
   (commit `5ca9271`). The remaining piece is the test-side synthetic-fixture
   builder for the 4 placeholder IQ3 tests (~1-2h on Strix Halo; see
   `.planning/notes/iq3-per-host-parity-deferred.md`).

3. **C1 audit — CUDA F16 attention sliding-window discriminator test**
   (BLOCKED on CUDA host). Mirror the Vulkan FA sliding-window
   discriminator on the CUDA F32 + F16 sides. See audit §C1.

4. **H6 — CUDA PTX regen** (BLOCKED on nvcc host). Patched `.cu` from
   `8e8c228` not yet recompiled; CUDA sliding-window models silently
   use buggy mask.

5. ~~**Vulkan Gemma 3 wiring**~~ — SHIPPED in session 5 (round-3 worktree merge
   `e55ade7`). See `<session_5_summary>` above.

6. **llama.cpp Vulkan FA bench** for the GAIA H2 acceptance metric
   (requires a llama.cpp build with `-DGGML_VULKAN=ON` on the Strix Halo
   host).

7. **Item 7 — Phase 9 Step 59 advanced scheduling** (multi-week).

8. **dotnet/runtime PR draft** — passive; waiting on @tannergooding
   response to https://github.com/dotnet/runtime/issues/86849#issuecomment-4472274791.

</remaining_work>

<blockers>

- CUDA host needed for: C1, H6.
- llama.cpp Vulkan build needed for: GAIA H2 comparison.
- Everything else is non-blocked.

</blockers>

<context>

Two parallel-worktree sessions completed (sessions 3 + 4). Pattern is
mature: spawn N agents in isolated worktrees with truly disjoint file
scopes, merge cleanly with `--no-ff`, verify with build + tests + push,
delete origin branches + local refs. Caveat from this session: agents
can bypass the worktree branch and push directly to feature/qwen3.6 if
their isolation mode permits it (Gemma 3 agent did so this session; the
auditor did the same last session). Tolerable as long as their work is
small + self-contained.

The audit-H3 trap-the-bug discovery (IQ3 dispatch arms wired but
unreachable due to missing upload predicates) is exactly the
class-of-bug the structural-mitigation discipline is supposed to catch.
Caught here only because the IQ3 host parity tests were written —
without them the dispatch would have stayed silently dead until a real
IQ3 GGUF hit the bench.

</context>

<next_action>

Highest-value non-blocked items:

- **IQ3 upload-path predicates** (~30 LoC, ~30 min) — unblocks the
  IQ3 host parity tests from this session.
- **Vulkan Gemma 3 wiring** (~half-day) — completes the Gemma 3 e2e
  forward path on the GPU side.
- **Per-sequence recurrent-state isolation** for Mamba-3 + Qwen3MoeHybrid
  (~half-day each) — unblocks Phase 5b/5f fusion for those hosts.

CUDA-host-dependent (C1, H6) and Phase 9 Step 59 wait their turn.

</next_action>
