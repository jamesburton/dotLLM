---
branch: feature/mamba-3
workstream: All 4 MLA Vulkan infra pieces + MoE top-k softmax + GEMV stride fix + device-local activations landed; MLA forward-pass integration is the next coherent commit
stage: v0.2.0-alpha.3 released (B2 rmsnorm+matmul fusion, 282 → 295 tok/s SmolLM-135M.Q8_0 decode); MLA + MoE infrastructure now in tree (5 standalone kernels parity-tested); forward-pass integration is the remaining piece
status: branch pushed through d88664f
last_commit: d88664f
last_tag: v0.2.0-alpha.3
pr: https://github.com/kkokosa/dotLLM/pull/136
last_updated: 2026-04-26

handoff_intent: pause — fresh session should resume MLA step 4 (forward-pass
  integration) as a single coherent unit: VulkanWeights MlaLayerBuffers
  upload, VulkanForwardState MLA scratch, RecordMlaLayer in
  VulkanTransformerModel, lift RejectUnsupportedArchitecture(MlaConfig),
  end-to-end test. All required kernels (MLA attention, MLA RoPE, MlaVulkanKvCache,
  MLA kv_b split) are committed and parity-tested.
---

<current_state>
v0.2.0-alpha.3 released (`3042369`, tag `4d7ec4f`) with B2 sub-tile-8
rmsnorm+matmul fusion default-on. Clean-conditions A/B/A showed 295
tok/s avg vs 282 unfused (+4.6%; beats v0.2.0-alpha.2 baseline of 291).

MLA Vulkan port (issue #3) — **4 of 4 infrastructure pieces** committed
and pushed:
- `432118d` MLA attention F32 kernel + 6 parity tests
- `91e4616` MLA RoPE F32 kernel + 6 parity tests
- `391f13b` MlaVulkanKvCache + 4 lifecycle tests
- `d88664f` MLA kv_b split kernel + 6 parity tests (the per-head
  reshape after kv_b matmul into K_nope and V buffers)

Other workstreams completed since v0.2.0-alpha.3:
- `a506a5f` Issue #1: matmul_q8_0 GEMV stride bug at K=32 with M>1 fixed
  (rowUints*4 → blocksPerRow*34, matching the GEMM and fused shaders).
  Bug exposed via 3 new regression tests (M=8/K=32, M=4/K=96, M=2/K=160).
- `98fee09` Issue #4 step 1: MoE top-k softmax kernel + 7 parity tests
  (Mixtral 8/2, Qwen1.5 raw probs, Qwen3 norm-topk, DeepSeek-V2 64/4,
  256/8 stress). Stable lower-index-wins tie-break preserved by single-
  threaded selection inside each WG.
- `5608f98` Vulkan activation scratch device-local: HiddenState slots,
  AttnOutput, SiluOutput moved to device-local memory. ~1.7% min-latency
  improvement on UMA Strix Halo (4 paired runs); likely meaningful on
  dGPU. Bias-add receivers (Q/K/V/NormOutput/FfnGate/FfnUp) stay host-
  visible — moving them needs `bias_add_f32` (issue #7).

Step 4 of MLA — forward-pass integration — has NOT been started. Repo
state is clean and pushed. All required kernels are in tree and
parity-tested; the remaining work is plumbing.
</current_state>

## Orientation

- **Branch:** `feature/mamba-3` at `d88664f`, pushed to `origin` (= `jamesburton/dotLLM`).
- **Last tag:** `v0.2.0-alpha.3` (https://github.com/jamesburton/dotLLM/releases/tag/v0.2.0-alpha.3). Notes have full perf table.
- **Upstream:** `kkokosa/dotLLM` (not pushed to from this branch).
- **Tests:** 1409 unit pass / 0 fail / 38 skip (CUDA/HIP, no GPU); 66 Vulkan unit + 1 Vulkan integration parity test (8/9 strict argmax CPU↔Vulkan), 1 opt-in perf harness (`DOTLLM_VULKAN_PERF=1`).

## Pause-work handoff (2026-04-25)

<completed_work>
Since v0.2.0-alpha:
- `32a15b0` Embedding table uploaded once (#12)
- `8705086` `VK_KHR_cooperative_matrix` probe at device init
- `b56a696` `matmul_q8_0_gemm_coopmat` kernel (790 GFLOPS, 3.8× scalar)
- `8d241c3` `attention_f32_coopmat` kernel (opt-in: `DOTLLM_VULKAN_USE_COOPMAT_ATTENTION=1`; off by default — measured slower on RDNA3.5 iGPU)
- `c8af81a` Forward routes Q8_0 prefill through coopmat GEMM
- `e6ce623` Hidden-state 2-slot swap eliminates 120 vkCmdCopyBuffer dispatches per decode + fixes a latent compute→transfer barrier bug
- `3f45231` **B2 fusion**: `rmsnorm_matmul_q8_0.comp` sub-tile-8 design, default-on. 282 → 295 tok/s decode (+4.6%; resilience win on max latency 7.91 → 5.70 ms avg). 8 parity unit tests + integration parity unchanged (8/9 strict argmax). Two earlier variants ruled out: per-row (ROWS_PER_WG=1) was ~7% slower (576× redundant rmsnorm overwhelmed dispatch+barrier savings); sub-tile-4 broke even.
- 12 GitHub issues filed on `jamesburton/dotLLM` capturing every flagged follow-up
- **Issue #8 ruled out**: R4 N>1 routing through `OuterProductGemmQ8_0(..., pool)` was correct but no measurable win on Zen 5 32T at SmolLM-135M shapes — reverted. Existing tiled GEMM with `VecDotQ8_0Avx512_4Rows` + `f9b1ff6` 2D row×token partition already saturates downstream of the matmul kernel.
</completed_work>

<remaining_work>
**Direct path to llama.cpp parity (349 tok/s on this iGPU; we're at 295 = ~1.18× gap):**
- **Activation scratch → device-local with async staging ring** (estimated 0.3-0.5 ms). Currently host-visible — flagged in `.perf-runs/vulkan-perf-wave-20260424/README.md` as the perf-wave's deferred follow-up.
- **Issue #2** Cooperative-matrix variant for the GEMV path (decode). Currently only GEMM has coopmat; GEMV is scalar. Could yield further matmul wins on coopmat hardware.

**Functional / model-coverage gaps (don't close perf gap, broaden support):**
- Issues #3 / #4 / #5 — MLA / MoE / SSM Vulkan ports
- Issues #6 / #7 — per-head QK-norm + bias-add kernels
- Issue #1 — latent GEMV stride bug at K=32 with M>1 (B2's fused shader sidesteps it for the fused matmul phase via `blocksPerRow*34` stride; standalone GEMV still has the bug)
- Issue #11 — subgroup A/B across vendors

**CPU residual (small):**
- Issues #9 / #10 — F32/F16 GemmTiled 2D partition, first-call noise.
- ~~Issue #8~~ — ruled out on this hardware (see decisions_made).
</remaining_work>

<decisions_made>
- **Coopmat attention default = OFF** on this hardware. Measured 1.27–1.56× SLOWER than shared-mem reduce because RDNA3.5 wave=64 underfills 16-lane WMMA tile + f32→f16 conversion overhead. Opt-in via `DOTLLM_VULKAN_USE_COOPMAT_ATTENTION=1` for NVIDIA tensor cores / AMD discrete (gfx110x) where physics flips.
- **Coopmat GEMM tolerance 5e-3 (not 1e-4)** — physical constraint of `KHR_coopmat` F16-only operands; not a defect. Coopmat kernel tests use the looser bar; integration test still passes 8/9 strict argmax.
- **B1 buffer swap** uses 2-slot rotation rather than 4 distinct buffers. Memory drops by ~25% on intermediate scratch; latent LM-head barrier bug (compute→transfer) surfaced and fixed in same commit.
- **B2 fusion default = ON** with sub-tile-8 design (ROWS_PER_WG=8, K_MAX=1024). Per-row variant lost ~7%; sub-tile-4 broke even; sub-tile-8 wins by amortising the redundant rmsnorm reduction across 8 matmul rows. K_MAX=1024 (4 KB sharedNorm) keeps shared-memory pressure low so many WGs stay in flight per CU. Models with hidden >1024 fall back to standalone (rmsnorm + matmul).
- **Issue #8 (CPU R4 prefill N>1) ruled out on Zen 5**. Routing N>1 Q8_0 prefill through `OuterProductGemmQ8_0(..., pool)` was correctness-clean but no measurable win — within noise of the existing path. Reverted. The kernel itself is fine; the existing 2D row×token partition already saturates this hardware. Could still be the right call on hardware where the existing path doesn't saturate.
</decisions_made>

<blockers>
None active. All agent worktrees cleaned up. Working tree clean.

Soft caveat: agents using `isolation: "worktree"` were observed writing to the main repo's working tree during execution and switching its HEAD via the shared `.git`. Recovery pattern that worked: let the agent finish, then cherry-pick its branch (sometimes with `--strategy-option=theirs` for combined-changes files), reset the auxiliary branch back to its agent-tip, run `git worktree remove -f -f` once done. Surgical `git checkout <branch> -- <files>` is also viable for picking up only specific new files when full cherry-pick conflicts. Document this if it persists.
</blockers>

<context>
The session had a clear "ROI ladder" framing (`.perf-runs/vulkan-next-version-plan.md`):
A1 coopmat GEMM → A2 coopmat attention → B1 buffer swap → B2 fuse → B3 embedding once.

A1 + B3 + B1 all delivered as predicted. A2 was correctness-clean but
**a perf regression on this hardware** — kept in tree for vendor portability via opt-in env var, the right call. The 1.44× win is mostly A1 (coopmat prefill GEMM, ~1.5× on prefill cascading into decode) + B1 (~120 dispatches eliminated, ~0.7-1.5 ms saved on decode).

**Mental model for next session:** the remaining 1.20× gap is no longer about kernel quality — it's about dispatch count / sync overhead. B2 (fusion) reduces dispatch count by ~10%. Activation-scratch device-local removes the last per-step host↔device traffic. Together likely close ~half of the remaining gap. Beating llama.cpp on this hardware probably requires one of: split-K GEMV, or a coopmat path that's optimised for AMD's wave=64 layout (not shipped by llama.cpp either, so this would be a research project).
</context>

<next_action>
1. **MLA Vulkan port (issue #3) — step 4: forward-pass integration.** All 4 standalone kernels (`432118d`, `91e4616`, `391f13b`, `d88664f`) are committed and parity-tested. This is the plumbing commit that ties them together. Touch points:
    - `src/DotLLM.Vulkan/VulkanWeights.cs` — add `MlaLayerBuffers` struct (Q_a/Q_b OR Q_proj monolithic, KV_a_proj_with_mqa, KV_b_proj, Q_a_layernorm + KV_a_layernorm device buffers, per-layer dims). Branch the per-layer Upload path on `TransformerLayerWeights.Mla` non-null. CPU loader already populates this when `MlaConfig` is non-null.
    - `src/DotLLM.Vulkan/VulkanForwardState.cs` — add MLA scratch buffers, lazy-grown: `MlaQLatent` ([seqLen, qLoraRank]), `MlaQLatentNorm`, `MlaQ` ([seqLen, numHeads * qkHeadDim]), `MlaKvCompressed` ([seqLen, kvLoraRank + qkRopeHeadDim]), `MlaKvLatentNorm`, `MlaKvBExpanded`, `MlaKNope`, `MlaV` ([seqLen, numHeads * vHeadDim]), `MlaKPe` ([seqLen, qkRopeHeadDim]), `MlaAttnOutput` ([seqLen, numHeads * vHeadDim]).
    - `src/DotLLM.Vulkan/VulkanTransformerModel.cs`:
        * Lift `RejectUnsupportedArchitecture(MlaConfig)` (line ~258).
        * Add `CreateMlaKvCache(int maxSeqLen)` returning a `MlaVulkanKvCache`.
        * New `RecordMlaLayer` orchestrating: rmsnorm → q_a/q_b matmul (or monolithic q_proj) → q_a_layernorm → kv_a matmul → kv_a_layernorm → kv_b matmul → `MlaKvSplitF32Kernel` (the kv_b split) → `RopeMlaF32Kernel` → `MlaVulkanKvCache.RecordUpdate` → `AttentionMlaF32Kernel` → o_proj. Then standard FFN (rmsnorm + Gate/Up + SwiGLU + Down + residual).
        * Branch in `Forward` to call `RecordMlaLayer` when `cpuLw.Mla != null`.
    - **Test:** synthetic small MLA layer parity vs CPU `MlaAttention.Execute`. Real-weight DeepSeek-V2-Lite integration test (gated on `DOTLLM_DEEPSEEK_V2_LITE_PATH`) is a separate follow-up.
2. **Issue #4 step 2+: Vulkan MoE forward integration.** Top-k softmax kernel landed (`98fee09`). Remaining: VulkanMoeLayerWeights (per-expert MLP buffers + router gate), VulkanForwardState MoE scratch (router logits, topk idx/wts, gather/scatter, expert intermediate), `RecordMoeLayer` orchestrating gate matmul → topk softmax → per-(token, slot) gather + matmul + SwiGLU + matmul + weighted scatter, lift `RejectUnsupportedArchitecture(MoeConfig)`, integration test against synthetic Mixtral-tiny.
3. **`bias_add_f32` kernel (issue #7)** — unblocks moving the remaining 5 activation buffers (Q, K, V, NormOutput, FfnGate, FfnUp) to device-local. Currently they stay host-visible because `AddBiasRows` host-maps them when biases are present. SmolLM has no biases; Phi-3 / Qwen3 / DeepSeek-V2 do.
4. **Vulkan coopmat GEMV decode (issue #2)** — currently only GEMM has coopmat; GEMV is scalar.

**Don't** do early:
- Revert any of today's commits (`a506a5f`, `98fee09`, `5608f98`, `d88664f`, etc.) — all correctness-clean and either parity-tested or backed by test additions.
- Revert B2 (`3f45231`) — clean perf win, default-on with opt-out env var.
- Revert v0.2.0-alpha.3 / -alpha.2 commits — correctness-pinned by tests.
- Re-enable coopmat attention by default — measured slower here.
- Touch the integration test tolerance — calibrated; tightening past 8/9 strict is a regression signal.
- Re-try issue #8 (R4 prefill N>1) without first establishing that the existing tiled-GEMM path doesn't saturate (not on Zen 5).
</next_action>
- **Working tree:** clean. `.perf-runs/` tracked except large ETLs/nettraces/speedscope (gitignored).
- This doc supersedes previous versions. `PLANS.md` remains the background gap inventory.

## Session arc (01d03a4 → d7accc7, ~24 commits)

### CPU decode: 8.66 → 33.76 tok/s (~4× speedup; within 3% of llama.cpp)

Cross-library OpenAI harness, SmolLM-135M Q8_0, 192 output tokens, cold run:

| Backend | toks/s | wall |
|---|---:|---:|
| llama.cpp CPU (`-ngl 0`) | 34.73 | 5.53 s |
| dotLLM CPU (after `173fd29`) | **33.76** | 5.69 s (cold), 4.84–4.92 s (warm prefix) |
| dotLLM CPU (before `173fd29`) | 8.66 | 22.17 s |

**Root cause:** `ComputeThreadPool._decodeThreadCount` fell back to `threadCount` (32) when no `NumaTopology` provided. PerfView kernel sampling showed 76% of dotLLM CPU in the `SpinWait` spin-loop — 31 threads hammering one cache line on `_dispatchGeneration` + contended `_completion.Signal()`. `ThreadPoolDispatchBenchmarks` (commit `e9c93d2`) quantified per-dispatch cost at 333 µs on 32T SpinWait vs 1 µs at 8T. One-line fix in `173fd29`: cap the default to `min(8, threadCount)`, matching the heuristic already used when a topology IS provided (memory channels × 2).

### CPU prefill: 334 → 898 tok/s (2.7×; gap 6.6× → 2.4×)

`PrefillBenchmarks` (pp512-equivalent, commit `ad507f0`). Root cause was NOT the expected cache blocking — weight tiles already fit in Zen 5's L2. Real cause: `GemmTiledQ8Worker` partitioned only by tile, so for narrow-M projections (QKV/O at 192–576 rows with `ComputeTileM=256`), `totalTiles` ∈ {1, 3} and 29 of 32 threads idled on the barrier. Commit `f9b1ff6` splits work across both M (rows) and N (tokens) when `totalTiles < threadCount`. Per-projection GFLOPS: Q/K/V 3.9–5.6×, Gate/Up 2.0–2.3×, Down 3.2×, lm_head 1.4× (wide M, already OK).

### Prefix-cache correctness fix (unblocked honest benchmarking)

Commit `53cee5d` — `ResolveKvCache` was accepting any cached entry whose `MaxLength ≥ promptLen`, with zero room for generation. After the server's warmup request primed the cache at ~34 slots, every subsequent chat completion silently truncated at ~12 tokens regardless of `max_tokens`. Fix clamps to `MaxLength ≥ promptLen + maxTokens`; regression test in `PagedKvCacheIntegrationTests`. Introduced in 17bc4383 (Step 36: Paged KV-cache). Only affected `/v1/chat/completions` because the harness's warmup chat primed the cache; `/v1/completions` was unaffected because no matching prefix.

### Full F32 Vulkan forward (10 kernels, end-to-end decode parity on SmolLM-135M)

Before this session: 1 kernel (`Add`). After: 10 kernels + a validated `VulkanTransformerModel` that decodes SmolLM-135M F32 weights and matches the CPU backend.

| Kernel | Commit | Notes |
|---|---|---|
| `matmul_f32` | `91ff03d` | 2D dispatch GEMM, 9 test shapes |
| `matmul_q8_0` (GEMV) | `97d7a7e` | Decode GEMV, full 49,152×576 lm_head = 892 ms |
| `rmsnorm_f32` | `a4c06ff` | Workgroup tree reduce |
| `rope_f32` + NeoX | `912e3d4`, `e807fd3` | Norm + NeoX (4 extra parity tests added in wave 3) |
| `attention_f32` | `268625e` | Flash-attention online softmax, TILE_KV=256 |
| `swiglu_f32` | `5ed52c3` | Trivial pointwise |
| `matmul_q8_0_gemm` | `d649171` | Batched Q8_0 matmul, ~209 GFLOPS at Llama-3 4096² N=64 |
| `rmsnorm_f32_sg`, `attention_f32_sg` | `19950b2` | Subgroup-arithmetic variants (probed + dispatched at runtime) |

All validated to abs 1e-4 / rel 1e-3 against CPU reference on AMD Radeon 8060S iGPU.

**Subgroup fast path finding (`81a23d0`):** numerically equivalent but *slower* on RDNA3.5 at WG=256 (attention 0.50×, rmsnorm 1.02× at SmolLM shapes) — barriers are cheap on modern AMD; extra register pressure + broadcast-slot barrier eats the log₂(256)=8 barriers saved. Kept in tree for future A/B on NVIDIA/mobile; default stays shared-mem. Toggle with `DOTLLM_VULKAN_FORCE_SHARED_REDUCE=1`.

**End-to-end Vulkan forward (`dc334d3`, `d7accc7`):** `VulkanTransformerModel` + `VulkanWeights` + `VulkanKvCache` + `VulkanForwardState`. Dequants Q8_0 to F32 at load (Q8_0 end-to-end is the next workstream). Rejects MLA/MoE/SSM at load time. 8/9 strict argmax matches CPU across prefill + 8 decode steps on `"The capital of France is"` (both backends → ` Paris`); the one "miss" is a top-2 rank swap from Q8_0-vs-FP32 precision noise, not a correctness bug (Vulkan path runs closer to an FP32 oracle).

**Descriptor-pool leak fix (`5490f34`):** all 7 wave-1/wave-2 kernels leaked descriptor sets — pool was `maxSets=1`, second `Launch` would die with `VK_ERROR_OUT_OF_POOL_MEMORY`. Per-kernel unit tests missed it (each test creates a fresh kernel); only the end-to-end integration surfaced it. Fix: `vkResetDescriptorPool` after `vkQueueWaitIdle` in every kernel's finally.

### Infrastructure

- **`.perf-runs/`** — text summaries + BDN reports tracked, large binaries (`*.etl`, `*.etl.zip`, `*.nettrace`, `*.speedscope.json`) gitignored.
- **`.perf-runs/tools/EtlStackDump/`** — small TraceEvent-based analyzer for PerfView ETLs; opts out of central package management so it doesn't pollute `Directory.Packages.props`.
- **`benchmarks/DotLLM.Benchmarks/ThreadPoolDispatchBenchmarks.cs`** — per-dispatch cost microbench across thread counts + dispatch modes; the measurement that forced the decode-thread-count diagnosis.
- **`benchmarks/DotLLM.Benchmarks/PrefillBenchmarks.cs`** — pp512-equivalent isolated prefill throughput benchmark.
- **`VulkanKernels` xUnit collection with `DisableParallelization=true`** (`TestCollections.cs`) so env-var toggles on the subgroup path don't race.
- **`glslc` on PATH** via scoop shim added this session (`~/scoop/shims/glslc.exe`). Shader edits can run `glslc <name>.comp -o <name>.spv` directly.

---

## Open items (check GitHub Issues — filed this session)

The open follow-ups have been filed as GitHub issues on `jamesburton/dotLLM` so they don't rot in this doc. Pull the issue tracker for the current state; the summary below is for orientation.

### Vulkan (GPU path — the biggest absolute remaining headroom)

- **Q8_0 end-to-end** — currently being worked on in parallel. Keep Q8_0 blocks on device, skip FP32 dequant at load; route matmuls through `matmul_q8_0` (GEMV) + `matmul_q8_0_gemm` (batched). Once this lands, Vulkan decode should beat CPU decode on this iGPU.
- **Perf wave** — currently being worked on in parallel. Device-local VRAM (staging ring), fence-based pipelining, descriptor pool reuse, remove `vkQueueWaitIdle` per kernel. Current Vulkan forward is correctness-first and runs in "seconds per forward" — not because the kernels are slow, but because every dispatch does a full host↔device round-trip.
- **Cooperative-matrix Q8_0 GEMM variant** — RDNA3 WMMA 16×16×16 int8 tiles via `VK_KHR_cooperative_matrix` should yield ~10× over current 209 GFLOPS on the batched kernel. Probe pattern from `e6f5e40` (subgroup probe) is the reference.
- **Arch ports**: MLA (DeepSeek-V2/V3), MoE (Mixtral, Qwen-MoE, DeepSeek-V2-MoE), SSM/Mamba-3/Nemotron-H hybrid. All rejected at load today.
- **Missing leaf kernels**: `per_head_rmsnorm_f32` for Qwen3 QK-norm; `bias_add_f32` for Phi-3-family biases (currently host-fallback).
- **Latent GEMV stride bug at K=32 with M>1** — `rowByteStride = rowUints * 4` is wrong when `blocksPerRow*34 % 4 != 0`. Not exercised by current tests; GEMM kernel works around it. File a test that exposes it + patch the GEMV.
- **Subgroup A/B across vendors** — on AMD RDNA3.5 the subgroup path is a loss; NVIDIA Ampere/Ada (subgroup=32) and mobile likely flip. `DOTLLM_VULKAN_FORCE_SHARED_REDUCE=1` lets per-vendor comparison happen without code changes.

### CPU (much smaller remaining headroom)

- **Prefill gap residual**: 2.4× to llama.cpp. Candidates: R4/interleaved path for N>1 prefill (currently decode-only); `GemmTiledF32Worker` / `GemmTiledF16Worker` need the same 2D-partition fix as Q8 got in `f9b1ff6`.
- **First-call-after-warmup noise** in `PrefillBenchmarks` — 3–5× on the first kernel call. Likely first-parallel-call scheduling / L1 cold on weight tiles. Visible as first-request latency at server start.

### Model/arch correctness (low priority, no current test failures)

- Long-context YaRN validation — `DeepSeekV2Lite_LongContext_LogitsMatchPyTorchReference` stub skips until `deepseek-v2-lite-longctx-reference.json` is generated (~32 GiB bf16 HF inference on ~4500 tokens, ~30 min wall).
- Secondary HF divergences: `routed_scaling_factor` (parsed but ignored; V2-Lite=1.0 no-op, V2-full/V3 need it); V3 `topk_method=group_limited_greedy` unimplemented.
- ROCm/HIP gfx1151: blocked on driver. Try WSL2 + `rocm/dev-ubuntu-24.04:6.4-complete`.

### CUDA

Separate machine; `CONTINUE_ON_CUDA.md` has the 11-step pickup guide. 36 CUDA unit tests activate once CUDA is available. MLA / MoE / Mamba-3 on CUDA is a multi-week port.

---

## Environment snapshot

- **Box:** Windows 11, Ryzen AI Max+ 395 (Zen 5, 32T), AMD Radeon 8060S iGPU (Strix Halo, gfx1151, RDNA3.5).
- **Vulkan SDK:** scoop-installed, `glslc` on PATH via shim added this session.
- **ROCm 7.1:** installed; iGPU runtime blocks `hipModuleLoadData` with `hipErrorInvalidKernelFile` — HIP path expected to work on discrete gfx1030/gfx1100 only.
- **CUDA:** not installed here.
- **Python venv:** `.venv-pyref/` (torch 2.4.1+cpu, transformers 4.45.0).
- **Real weights cached:**
  - `C:/Users/james/.dotllm/test-cache/QuantFactory/SmolLM-135M-GGUF/SmolLM-135M.Q8_0.gguf` — primary perf target
  - `C:/temp/dotllm-tinyllama/` (2.1 GB)
  - `C:/temp/dotllm-phi35-mini/` (7.6 GB)
  - `C:/temp/dotllm-granite31-moe/` (6.3 GB)
  - `C:/temp/dotllm-deepseek-v2-lite/` (30 GB)
  - `C:/Users/james/.cache/huggingface/hub/models--Qwen--Qwen2.5-0.5B/...`
- **llama.cpp:** `C:/Development/llama.cpp/llama-{cli,bench,server}.exe` (build d0a6dfeb2, Vulkan).
- **Cross-lib benchmark harness:** `C:/Development/OllamaBenchmarks/scripts/benchmark_throughput_openai.py`.
- **Agent worktrees** live at `C:/Development/dotLLM/.claude/worktrees/` — cleanup with `git worktree remove -f -f <path>` after confirming commits land on `feature/mamba-3`.

---

## Suggested first actions

1. `git pull origin feature/mamba-3` — pull to `d7accc7` or later.
2. `dotnet test tests/DotLLM.Tests.Unit/` — expect 1396 / 0 / 38.
3. `dotnet test tests/DotLLM.Tests.Integration/ --filter ~Vulkan` — expect 1 / 0 on AMD/Intel/NVIDIA box with Vulkan 1.1+ support.
4. Check GitHub issues on `jamesburton/dotLLM` for filed follow-ups.
5. The three biggest levers open: (a) **Q8_0 Vulkan end-to-end** (in flight), (b) **Vulkan perf wave** (in flight), (c) **cooperative-matrix Q8 GEMM**. (a) likely lands first and will need a re-benchmark vs llama.cpp Vulkan's 392 tok/s decode number.

## DO NOT do early

- Merge `main` into this branch — `main` is upstream's and doesn't know about any of this work.
- Delete agent worktrees without confirming commits landed on `feature/mamba-3` (cherry-picks during this session renamed hashes; always `git log --oneline -10` before deleting).
- Revert these correctness fixes (they're pinned by regression tests):
  - `745b091` HF Llama RoPE Norm → NeoX
  - `53cee5d` prefix-cache undersized-reuse fix
  - `5490f34` Vulkan descriptor-pool reset per Launch
- Revert `173fd29` (decode-thread-count cap) — 4× CPU decode win, zero correctness cost, benchmarked.
- Revert `f9b1ff6` (2D prefill partition) — 2.7× CPU prefill win.
- Try to "close the llama.cpp gap with an MMQ kernel" — the fused MMQ path is already in place (`VecDotQ8_0Avx{2,512}_4Rows` multiply Q8_0×Q8_0 directly with per-block scale FMA). Both decode and prefill gaps were about thread coordination, not kernel quality.
- Regenerate existing reference JSONs under `tests/DotLLM.Tests.Integration/Models/Loaders/references/` — they're the HF oracle.
