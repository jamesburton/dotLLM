---
branch: feature/mamba-3
workstream: architecture breadth expansion (MLA + MoE variants + streaming + real-weight validation + PLANS.md)
stage: Wave 0 complete (all immediate deliverables shipped); Wave 1 of PLANS.md not yet started
status: paused — context at 78%, awaiting fresh session to dispatch next wave
last_commit: 3e4348e
pr: https://github.com/kkokosa/dotLLM/pull/136  (draft)
last_updated: 2026-04-22 16:18
---

<current_state>
Paused mid-session after a productive burst. All major capability work is committed
and pushed; **PLANS.md at repo root** is the authoritative inventory of remaining
gaps (coverage, standards, docs) organised by ROI with parallelization analysis.

`DeepSeek-V2-Lite` real-weight download is in flight (9.3 GB / ~31 GB so far,
task `b3rt1lmmu`, fetching to `C:/temp/dotllm-deepseek-v2-lite`). When complete,
the existing `DeepSeekV2Lite_LoadsAndForwardsEndToEnd` test pattern needs adding
(PLANS item **P0.2**) to validate MLA + multi-shared-expert at real scale.

Feature branch is at `3e4348e` on `origin/feature/mamba-3`, clean working tree
except `.claude/scheduled_tasks.lock` (untracked).
</current_state>

<completed_work>

Since resume-work reactivated this session, pushed commits on `feature/mamba-3`:

- `3e4348e` PLANS.md — full gap inventory + parallelization analysis
- `b72f6a0` Tests: Qwen2.5-0.5B + TinyLlama-1.1B real-weight end-to-end
- `bf4613d` docs: Phi-3 + Granite-MoE real-weight loaders
- `6c8c662` Granite-MoE loader: fused-per-expert input_linear/output_linear
- `006935b` Phi-3 loader: split fused qkv_proj / gate_up_proj tensors
- `45d21cf` docs: DeepSeek MLA TransformerModel integration
- `3757325` DeepSeek-V2/V3 end-to-end MLA integration
- `c834ef8` docs: DeepSeek multi-shared-expert MoE
- `5aafc3c` MoE: multi-shared-expert (DeepSeek-V2/V3)
- `267a5ad` Tests: real HF checkpoint end-to-end harness
- `eb5277c` docs: Mamba-3 MIMO streaming decode
- `48cc32a` Stage 3/3 MIMO streaming: Block integration + tests
- `c68e3f0` Stage 2/3 MIMO streaming: ExecuteMimoStreaming kernel
- `85c3164` Stage 1/3 MIMO streaming: k_state rank-aware [R, H, N]
- `a03d853` docs: MoE GroupedGEMM refactor
- `17f0910` MoE GroupedGEMM (bit-identical)
- `1ebf9ff` docs: MLA attention
- `9a324e6` MLA attention kernel + DeepSeek-V2/V3 config detection

**Real-weight end-to-end validations** (all PASS with finite logits):
- `ib-ssm/mamba3-370M-10BT` (1.55 GB) — Mamba-3, streaming decode, text generation
- `microsoft/Phi-3.5-mini-instruct` (7.6 GB) — fused QKV/gate-up
- `ibm-granite/granite-3.0-3b-a800m-instruct` (6.3 GB) — Granite MoE fused-per-expert, 40 experts top-8
- `Qwen/Qwen2.5-0.5B` (999 MB) — Qwen2 dense, heavy GQA, tied embeds
- `TinyLlama/TinyLlama-1.1B-Chat-v1.0` (2.1 GB) — Llama-family

**Test counts**: 1285 unit pass, ~110 integration pass. PR #136 has a comment
per major unlock.
</completed_work>

<remaining_work>

**See `PLANS.md` at repo root for the full organized breakdown.** Summary:

- **P0 (blocks claims)**: ByteLevel BPE tokenizer; DeepSeek-V2-Lite real-weight
  test; Mamba-3 MIMO weight loader + model wiring; unified SUPPORTED_MODELS.md.
- **P1 (standards)**: shared attention tensor loader abstraction; remove `goto
  FfnBranch`; benchmark regression sweep; analyzer/style pass.
- **P2 (hardening)**: transformer generation loop tests; YaRN mscale; MLA
  absorption + latent KV-cache; sliding-window attention test; OLMoE real-weight;
  bf16/F16 correctness test.
- **P3 (docs)**: ATTENTION.md MLA section; TOKENIZERS.md HF adapter section;
  per-arch config ref; stale docs sweep; Qwen/TinyLlama News entry; PR #136
  description refresh.
- **P4 (deferred/blocked)**: chat templates, quantized safetensors, MIMO
  real-weight (no public checkpoint).

**Downloads in flight**: `deepseek-ai/DeepSeek-V2-Lite` (~31 GB, 9.3 GB cached).
</remaining_work>

<decisions_made>

- MLA integration shipped as PoC (no latent KV-cache, no absorption, no YaRN)
  — explicit follow-ups in PLANS P2.2 / P2.3.
- GroupedGEMM refactor is bit-identical — the bit-identity test is the
  existing MoE kernel synthetic tests (they assert against expected outputs
  computed from the old scalar algorithm).
- `Mamba3TransformerModel` still throws `NotSupportedException` for MIMO —
  kernel + block paths are complete and test-covered; only loader wiring is
  missing. PLANS P0.3.
- Qwen2.5-0.5B + TinyLlama tests don't exercise their tokenisers (only
  load+forward on token IDs) — ByteLevel BPE tokenizer path still untested.
  PLANS P0.1.
- `HF_HUB_DISABLE_XET=1` is required for reliable HF downloads on this
  Windows machine — XET backend has a "Background writer channel closed"
  bug that crashes mid-transfer. Keep using classic HTTP.
- Deleted `Qwen/Qwen1.5-MoE-A2.7B` (14 GB) mid-download — user noted
  Qwen3.6 is current. Replaced with Granite-3.0-MoE + potentially
  OLMoE-1B-7B (PLANS P2.5).
</decisions_made>

<blockers>

- **Context at 78%** — current session should not dispatch more complex work
  before compacting / fresh start.
- **DeepSeek-V2-Lite download** in progress, slow (HF rate limits + periodic
  hibernate stalls). Check `C:/temp/dotllm-deepseek-v2-lite` size. Background
  task `b3rt1lmmu`. Resume with `HF_HUB_DISABLE_XET=1 .../python.exe -c
  "snapshot_download(..., allow_patterns=['*.json','*.safetensors'])"` if
  stalled.
- **Windows hibernate** has stalled downloads ≥5 times this session. Works
  when machine is actively in use. Consider disabling sleep for long
  downloads.
- **No public Mamba-3 MIMO checkpoint** exists — P0.3 covers code-level
  wiring; real-weight proof awaits upstream release.
</blockers>

<context>

- The **PLANS.md parallelization analysis** (bottom of that file) gives a
  clear 5-wave dispatch plan:
  - W1 (5 parallel agents, ~30-60 min each): P0.1 ByteLevel tokenizer, P0.3
    Mamba-3 MIMO loader, P0.4 SUPPORTED_MODELS.md, P1.1 shared attention
    loader, P1.3 benchmarks
  - W2 (serial on `TransformerModel.cs` + `MlaAttention.cs`): P1.2 goto
    removal → P2.2 YaRN → P2.3 latent KV + absorption
  - W3 (parallel after W1): P2.1 transformer gen tests (needs P0.1),
    P2.4 sliding window, P2.5 OLMoE, P2.6 bf16 correctness
  - W4 (parallel, all docs): P3.1–P3.6
  - W5 (final): P1.4 analyzer pass
- First-wave dispatch is the recommended next step on resumption. The 5
  items are on disjoint files; worktree isolation optional (in-place is
  fine with short sequential dispatches).
- `DeepSeek-V2-Lite` test (P0.2) runs **automatically** via the gated
  integration test once the download's completeness check passes — no
  agent action required other than confirming the test picks it up.
- Every commit this session has a PR #136 comment. PR description has NOT
  been refreshed to reflect full scope (PLANS P3.6).
</context>

<next_action>

When resuming in a fresh session:

1. Run `git log --oneline -5` + `du -sh C:/temp/dotllm-deepseek-v2-lite` to
   confirm state and download progress.
2. If DeepSeek-V2-Lite download is complete, add the gated
   `DeepSeekV2Lite_LoadsAndForwardsEndToEnd` test (PLANS P0.2) — trivial
   addition to `RealHfSafetensorsEndToEndTests.cs` mirroring the existing
   patterns. Expected: MLA + multi-shared-expert at real scale.
3. Dispatch **Wave 1** from PLANS.md: 5 parallel in-place agents for P0.1
   (ByteLevel tokenizer), P0.3 (Mamba-3 MIMO loader), P0.4
   (SUPPORTED_MODELS.md), P1.1 (shared attention loader), and P1.3
   (benchmark sweep). All disjoint files — no isolation needed, but
   serial-dispatch each with a short prompt.
4. After W1 lands, chain W2 on `TransformerModel.cs` / `MlaAttention.cs`
   sequentially.
5. W3 docs + tests can run fully parallel after W1.
</next_action>
