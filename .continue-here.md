---
branch: feature/mamba-3-cuda (rebased onto feature/mamba-3 @ db09d46 — was 01d03a4)
workstream: CUDA decode optimisation + DeepSeek enablement — Graphs (default ON), QKV/Gate-Up fusion, KvUpdate fusion, KV-quant graph, MMQ Q4_K/Q5_K/Q6_K (dp4a), MMVQ-large + pre-Q8_1 (default ON for k≥1024), Norm rewrite, weight dedup, per-row K-quant embedding, dynamic MMQ shmem, **CUDA MLA Phase A (F32 + FP16) + Phase B latent + W_UK absorbed**, **CUDA MoE FFN F32**, **MLA + MoE dispatch in CudaTransformerModel.Forward (parity-tested 5e-2 vs CPU)**, **GPU mutex (scripts/gpu-lock.sh) for parallel sub-agents** all landed
stage: 225.9 tok/s graph / 117.7 tok/s eager on SmolLM-135M Q4_K_M (was 87.5 baseline = **2.58×** / 1.34×). Qwen3-4B Q4_K_M @ k=2560 → **54.4 tok/s graph** (was 19.6, 2.78×). **Qwen3-8B Q4_K_M @ k=4096 → 33.2 tok/s graph** (was 11.5, 2.89×; inside llama.cpp's reported 25-42 tok/s range). **191/202 GPU tests green** (was 100 pre-rebase; +3 new MLA-dispatch parity tests + parent's Vulkan additions). **End-to-end MLA + MoE dispatch on CUDA validated** (synthetic DeepSeek-V2 fixture: MLA + dense FFN with LoRA-Q, MLA + dense FFN with monolithic-Q V2-Lite, MLA + alternating-MoE all match CPU oracle within 5e-2). Real DeepSeek-V2-Lite Q4_K_M still requires Tier 3b/3c (quantized MLA + MoE weight paths).
status: rebased + Tier 1+2 commits LOCAL ONLY (origin/feature/mamba-3-cuda is stale at pre-rebase 7cc7b46 — needs force-push-with-lease before next session). Model cache lives on E:\.dotllm with junction at C:\Users\james\.dotllm. 5 GGUFs cached: SmolLM-135M.{Q4_K_M, Q8_0}, Qwen3-{0.6B, 4B, 8B}-Q4_K_M. Parallel sub-agents serialise GPU access through `scripts/gpu-lock.sh`.
last_commit: 471ec81
pr: https://github.com/jamesburton/dotLLM/pull/13 (this branch, stacked on feature/mamba-3 fork-internal); https://github.com/kkokosa/dotLLM/pull/136 (parent → upstream main)
last_updated: 2026-04-27 (Round 14 — Phase B grouped-GEMV Q5_K + Q6_K + Q8_0 variants)

---
branch: feature/mamba-3 (parent)
workstream: P2.6 HF parity + GPU backend scaffolding + MLA Phase B/C + YaRN long-context
stage: all 5/5 argmax vs HF on 4 real-weight archs; Vulkan/HIP scaffolded; YaRN wired; perf gap characterised
status: paused at 87% context — all work committed + pushed to origin
last_commit: e7b4aef
pr: https://github.com/kkokosa/dotLLM/pull/136
last_updated: 2026-04-26

handoff_intent: pause — fresh session should re-dispatch the heavy
  VulkanMamba3TransformerModel SISO wiring agent. Two earlier wiring-agent
  attempts hit context limits (one after 30 min of useful work as the
  NemotronH precedent at 4abe4c2; one bailed pre-write at 79% on entry).
  Spec is correct; just needs a fresh ~2k-LOC budget. After wiring, MIMO
  scan kernel (already in tree at 36e0c0a) wires in via a small follow-up.
---

## Orientation — CUDA branch (current)

- **Branch:** `feature/mamba-3-cuda` at `7cc7b46`, pushed to origin.
- **Box:** Windows 11 + NVIDIA RTX 3060 12 GB (SM 8.6). Driver 591.86 → CUDA 13.1 runtime. **CUDA Toolkit 13.1 installed at `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.1`** (note: NOT `E:\` per earlier .continue-here note). VS 2022 BuildTools provides cl.exe via vswhere. `native/build_ptx.bat` works.
- **CUDA 13 DLL layout gotcha:** runtime DLLs (cublas, cudart, nvrtc) moved from `bin\` to `bin\x64\` and the installer no longer adds that subdir to PATH. Resolver handles it (commit `55b2a65`). If shells inherit a stale `CUDA_PATH=v11.8`, set `$env:CUDA_PATH = "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.1"` once.
- **Model cache relocation:** `~/.dotllm/` is now a **junction** to `E:\.dotllm\` (created with `mklink /J`). Disk-free on `C:` was critical (~1.4 GB) and would not have fit Qwen3-8B (~5 GB). All apps continue to see `~/.dotllm/...` as before; downloads land on E:.
- **Test fixtures cached** (now at `E:\.dotllm\models\` via junction): SmolLM-135M.{Q4_K_M,Q8_0}.gguf, Qwen3-0.6B-Q4_K_M.gguf, Qwen3-4B-Q4_K_M.gguf, Qwen3-8B-Q4_K_M.gguf — all five exercised this session. HF safetensors for TinyLlama/Phi-3.5/Qwen2.5/DeepSeek-V2-Lite live on the Ryzen+iGPU box; `CudaTransformerModel.LoadFromSafetensors` (`919c238`) is wired so they would activate the 4 skipped HF-parity gates if/when they land here.
- **GPU access mutex** (`scripts/gpu-lock.sh`, commit `0a49dce`): single-holder file-based lock at `.gpu-lock/` that serialises GPU-touching commands (PTX builds, `dotnet test --filter Category=GPU`, `profile-cuda-decode` benchmarks) across concurrent sub-agents. Pattern: `bash scripts/gpu-lock.sh acquire <name> <reason> 1200 && <gpu-cmd>; bash scripts/gpu-lock.sh release <name>`. Stale-detected by timestamp (default 30 min). Non-GPU work (code editing, doc writes, design) is fully parallelisable and MUST NOT take the lock. Status check: `bash scripts/gpu-lock.sh status`.

## Suggested next actions (post Round 14)

Three concrete follow-ups remain. Pick whichever fits the available
session length — none blocks the others. All three should be done in
foreground (not via spawned worktree agents) until follow-up #2 lands.

Before doing GPU work in any new PowerShell session, run **once** to
unstick the v11.8 stale env (see follow-up #3):

```powershell
$env:CUDA_PATH = "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.1"
```

### 1. down_proj grouped-GEMV (~4-6 h focused work)

**Goal**: extend the Phase B grouped path to cover the V2-Lite
down_proj projection (K=intermediate=1408, M=hidden=2048). down_proj
is the third of three MoE projections per expert; gate + up already
go through grouped GEMV after Round 14, but down still falls through
to the dequant-then-GEMM fallback because K=1408 is not a multiple
of 256 (5.5 super-blocks per row).

**Two viable strategies — pick one**:

- **Strategy A (recommended): pad-on-upload**. At weight-upload time
  in `CudaMoeWeightsLoader`, pad each down_proj row from K=1408 to
  K=1536 (next multiple of 256, +9% extra bytes per expert). The
  trailing 128 elements become zero-quantized (block bytes filled
  with the quantization's neutral encoding — for Q4_K that's
  `d=0, dmin=0, scales=0, qs=0`; for Q5_K + Q6_K + Q8_0 similar).
  Input `x` is also padded to K=1536 with zeros at GEMV-input-quant
  time. Cost: ~9% extra weight bytes per expert (~11 MB extra per
  V2-Lite layer = ~290 MB extra across all 26 MoE layers).
  Compatibility: zero kernel changes; the existing four
  `moe_grouped_gemv_q*_k_f16` entries handle it as-is.

- **Strategy B: kernel-level partial-tail handling**. New kernel
  variants (`moe_grouped_gemv_q4_k_tail_f16` etc.) that take an
  extra `K_tail_elems` parameter and compute a final partial
  super-block contribution from a 128-element tail (Q4_K: 5
  whole super-blocks of 256 + 1 tail block of 128). More code, no
  extra weight bytes. Tail block decode logic mirrors the second
  half of a normal super-block.

**Recommended path is A** — saves all the kernel work and is the
exact compaction strategy llama.cpp uses for unaligned K. The 9%
weight overhead is irrelevant once you're already loading 8+ GB of
expert weights.

**Implementation steps (Strategy A)**:

1. Read `src/DotLLM.Cuda/CudaMoeWeightsLoader.cs` to find where
   per-expert raw quant bytes are uploaded. Add a `paddedK` helper:
   `paddedK = (K + 255) & ~255`.
2. For each per-expert row, compute the row's source bytes, then
   write `paddedK / superblock × blockBytes` bytes to GPU — first
   the real `K / 256 × blockBytes`, then `(paddedK - K) / 256` neutral
   blocks for the padding tail. The neutral encoding for each quant
   type:
   - Q4_K: 144 bytes of zeros (d=0, dmin=0 ⇒ all-zero contribution).
   - Q5_K: 176 bytes of zeros.
   - Q6_K: 210 bytes of zeros (d=0 ⇒ all-zero contribution).
   - Q8_0: 34 bytes of zeros (d=0).
3. In `CudaMoeFfn.Forward` where down_proj is dispatched, change
   the K passed to `LaunchMoeGroupedGemv` to `paddedK` and flip the
   fallback gate from `(hidden % 256) == 0` to also accept down_proj
   when `paddedK == intermediate_padded`.
4. Allocate the GEMV input scratch (`scratch.GemvInputF16`) at
   `paddedK` size instead of `K` size; ensure padded entries are
   written as `__float2half(0.0f)` after the F32→F16 quantize step
   for the real elements.
5. Add a CudaMoeGroupedGemvTests theory parameterized on
   `(M=hidden=2048, K=intermediate=1408, padded=true)` for each of
   the four quant types. The oracle is still `LaunchQuantizedGemv`
   over per-expert raw bytes (no padding needed for the oracle —
   it handles K=1408 directly because it doesn't have the K%256
   constraint).
6. Re-run the 27-layer Q3_K_M smoke and the V2-Lite Q4_K_M smokes
   (2/4/8-layer). Expect a small decode latency reduction (~5-10%
   per active expert × 6 active = ~10-15% per MoE layer).

**Validation gate**: existing 22 tests still green + 4 new padded
parity tests (one per quant type) within 1e-3 max-abs-diff.

**Risk**: forgetting to zero the input scratch padding tail. The
neutral weight encoding cancels the multiply, but only if `x[k]`
is also zero at the padded indices. Add an explicit
`cuMemsetD16Async` on the padded tail of `GemvInputF16` after each
F32→F16 input convert.

### 2. Worktree-spawn base SHA mismatch (~2-3 h focused work)

**Goal**: agents spawned with `isolation: "worktree"` should branch
off the *current* feature-branch tip, not whatever SHA the spawn
template defaults to (currently `c709a22` = `origin/main` on the
public-release `kkokosa/dotLLM` repo).

**Symptoms observed in Rounds 12 + 13**: spawned agent runs
`git status`, sees it's on a stale tree without our recent commits,
self-diagnoses correctly, and stops without doing damage. No work
loss but blocks parallelism.

**Root cause hypothesis**: the agent template invokes something like
`git worktree add <path> -b <branch> origin/main` rather than
`git worktree add <path> -b <branch> HEAD` from the parent repo's
current branch. Need to read the agent-spawn implementation to
confirm.

**Investigation steps**:

1. Find the agent worktree spawn code:
   - `grep -rn "git worktree add" .claude/` — the tool that creates
     them is most likely under `.claude/` (it's a local Claude Code
     feature, not a project-level script).
   - If not under `.claude/`, the spawn happens inside the Claude
     Code runtime (out of repo) — in that case, the workaround is
     the agent prompt: tell it to `git fetch && git checkout
     feature/mamba-3-cuda && git reset --hard origin/feature/mamba-3-cuda`
     after entering the worktree.
2. Inspect the latest leaked worktree under `.claude/worktrees/` to
   see what branch + base it was actually created on — that pins
   down the bug:
   ```bash
   ls .claude/worktrees/
   for d in .claude/worktrees/*/; do
     echo "=== $d ==="
     git -C "$d" log --oneline -1 2>/dev/null
     git -C "$d" branch --show-current 2>/dev/null
   done
   ```
3. If the spawn template is under `.claude/`, patch it to base off
   `HEAD` of the parent repo instead of `origin/main`. Likely a
   one-line change.
4. If the bug is in the runtime, document the agent-prompt workaround
   in this `.continue-here.md` operational gotchas section so future
   sessions can copy-paste it. Pattern:
   ```
   You are operating in a worktree at <path>. Before doing any work,
   run: cd <path> && git fetch origin && git reset --hard origin/feature/mamba-3-cuda
   to ensure you have the current branch tip.
   ```
5. Re-attempt parallel agents (e.g., a foreground main + one
   background sub-agent) to confirm the fix; don't claim the lock
   from both at once — the GPU mutex (`scripts/gpu-lock.sh`) handles
   serialization.

**Acceptance**: a spawned worktree-isolated agent on this branch
sees commit `923fb21` (or current tip) as `git log -1` from the
moment it starts, and can pick up new local commits made since the
worktree was created with a `git fetch && git reset --hard
origin/feature/mamba-3-cuda`.

### 3. CUDA_PATH stale-env ergonomics (~1 h, optional polish)

**Goal**: stop having to re-set `CUDA_PATH` per fresh PowerShell
call. Currently the user's shell-level env has `CUDA_PATH=v11.8`
(stale; CUDA 13.1 is installed at v13.1). The `CudaLibraryResolver`
only probes via env vars, so any test that loads cublas DLL fails
in a fresh PS session unless `CUDA_PATH` is overridden in-call.

**Three options, in increasing invasiveness**:

- **Option A (recommended)**: extend `CudaLibraryResolver` to also
  probe well-known install dirs as a fallback. Read
  `src/DotLLM.Cuda/Interop/CudaLibraryResolver.cs` and add (after
  the env-var probes):
  ```csharp
  // Fallback: probe standard CUDA install dirs on Windows. Useful
  // when CUDA_PATH points at a stale (e.g. v11.8) install but a
  // newer toolkit is installed alongside.
  if (OperatingSystem.IsWindows())
  {
      string baseDir = @"C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA";
      if (Directory.Exists(baseDir))
      {
          // Newest version first (lexical sort; v13.1 > v11.8).
          foreach (var dir in Directory.EnumerateDirectories(baseDir)
              .OrderByDescending(d => d, StringComparer.OrdinalIgnoreCase))
          {
              yield return dir;
          }
      }
  }
  ```
  Test: in a fresh PS without `$env:CUDA_PATH` override, run
  `dotnet test --filter "FullyQualifiedName~MlaForward_DenseFfn_LoRAQ"`
  — should pass.

- **Option B**: persist `CUDA_PATH=v13.1` to the user's environment
  via `[Environment]::SetEnvironmentVariable("CUDA_PATH",
  "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.1",
  "User")`. One-time fix; requires user consent because it changes
  shell-level state outside the repo.

- **Option C**: do nothing in code; document the gotcha in
  `docs/CUDA.md` operational notes. This is the current state.

**Recommended path is A** — codifies the resolution so neither the
user nor future agents need to remember the workaround. Adds ~10
lines of fallback probing code; no behavior change when `CUDA_PATH`
is already correct.

**Validation**: after the change, run the regression sweep without
setting `$env:CUDA_PATH`:
```powershell
dotnet test tests/DotLLM.Tests.Unit/DotLLM.Tests.Unit.csproj -c Release `
  --filter "FullyQualifiedName~CudaTransformerMlaForwardTests" -nologo
```
should pass without the env-var override.

### Order of recommendation

1. Do **#3 first** (smallest, removes friction for #1's testing).
2. Then **#1** (highest user value: real perf win on V2-Lite decode).
3. Then **#2** (unblocks future parallel-agent work, but
   foreground execution is a fine workaround for now).

### Pre-flight per session

```bash
git fetch origin && git status                # confirm at 923fb21 or newer
bash scripts/gpu-lock.sh status               # should be FREE
```

If the lock shows held by a stale name (older than ~30 min), the
script auto-detects and releases on the next acquire. If a worktree
under `.claude/worktrees/` shows uncommitted work, investigate
before deleting (per the "Operational gotchas" rule).

## Round 14: Phase B grouped-GEMV — Q5_K + Q6_K + Q8_0 variants

The Round-12/13 follow-up for non-Q4_K grouped-GEMV is now closed.
The Phase B cross-expert grouped GEMV kernel covers all four common
K-aligned quant types — Q4_K, Q5_K, Q6_K, Q8_0. Models with mixed
expert quantization no longer fall back to per-expert direct-GEMV for
the non-Q4_K majority.

### Commits this round (`4520e94..471ec81`)

```
471ec81 CUDA: Phase B grouped-GEMV — Q5_K + Q6_K + Q8_0 variants
```

### What landed

**Three new PTX kernels** in `native/kernels/moe_grouped_gemv.cu`,
each mirroring the corresponding `quantized_gemv_*` inner loop body
exactly so the per-call oracle and the grouped path produce
near-identical FP16 outputs:
- `moe_grouped_gemv_q5_k_f16` — 176-byte super-blocks, 8 sub-blocks of
  32, interleaved lo/hi with 1-bit `qh` append per element, packed
  6-bit scales (low-nibble + 2 high bits).
- `moe_grouped_gemv_q6_k_f16` — 210-byte super-blocks, two 128-element
  halves with `ql[128]` + `qh[64]` + `int8 scales[16]` + `half d` at
  offset 208.
- `moe_grouped_gemv_q8_0_f16` — 34-byte / 32-element regular blocks,
  `half d × Σ qs[j] · x[j]` per block.

**Dispatch wiring** (`src/DotLLM.Cuda/CudaKernels.cs`):
- New func fields + per-quant-type module-load via `TryGetFunction`
  (a stale PTX missing one entry still serves the others — gate the
  call with `HasMoeGroupedGemv(qt)`).
- New `HasMoeGroupedGemvQ5K / Q6K / Q8_0` properties, dispatch cases
  in `HasMoeGroupedGemv(qt)` + `LaunchMoeGroupedGemv`.
- `DOTLLM_DISABLE_MOE_GROUPED_GEMV` env still kills all four variants
  globally for A/B comparison.

**Tests** (`CudaMoeGroupedGemvTests`):
- Three new theories (Q5_K, Q6_K, Q8_0) × 3 shapes each = 9 new
  parity tests against the per-call `LaunchQuantizedGemv` oracle.
  All within 1e-3 max-abs-diff on F16 outputs (peak observed 0.0
  for the K-quants, ~1e-4 for Q8_0 due to FP32 reduction-order
  noise).

### Validation

22/22 unit tests green:
- 3 Q3_K CPU dequant
- 4 Q4_K grouped (existing)
- 3 Q5_K grouped (NEW)
- 3 Q6_K grouped (NEW)
- 3 Q8_0 grouped (NEW)
- 3 synthetic CudaTransformerMlaForwardTests (CPU↔CUDA parity)
- 3 real-GGUF Q4_K_M smokes (2/4/8-layer V2-Lite)

Plus the full 27-layer Q3_K_M smoke (separate filter; 42 s).

Build: 0 warnings, 0 errors.

### Operational notes

**CUDA_PATH gotcha**: each fresh PowerShell tool invocation inherits
the user's persisted `CUDA_PATH=v11.8` env var. Without
`$env:CUDA_PATH = "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.1"`
set in the same call, `LoadFromSafetensors` and `LoadFromGguf` both
fail with `System.DllNotFoundException: 'cublas'` because
`CudaLibraryResolver` only probes via env vars. The PTX-build script
bakes the correct path internally; the test invocation does not.
Workflow: prefix any `dotnet test` PowerShell call that touches
cublas/cudart with the env-var assignment.

### Open follow-ups

- **Down_proj grouped-GEMV** — the V2-Lite down_proj path is still
  the dequant-then-GEMM fallback because K=intermediate=1408 isn't
  256-aligned. A specialised grouped-GEMV-with-tail variant would
  need to handle a partial-trailing-block tail (or the kernel could
  pad to 256-aligned at upload time, trading ~9% extra weight bytes
  for grouped-path eligibility). Estimated 4-6 h focused work; not
  required for any current model.
- **Worktree-spawn base mismatch** — still unfixed at the spawn
  template; agents that try to take the lock and start an isolated
  worktree branch off `c709a22` (origin/main on `kkokosa/dotLLM`)
  rather than the current feature tip. Foreground execution is the
  workaround and worked cleanly for both Round 13 and Round 14.

## Round 13: Q3_K + Q4_1 + Q5_1 dequant — full V2-Lite-Q3_K_M end-to-end on RTX 3060

The Q3_K loader gap surfaced in Round 12 is closed. The 8.13 GB
`DeepSeek-Coder-V2-Lite-Instruct-Q3_K_M.gguf` now loads, uploads, and
runs all 27 layers (1 dense + 26 MoE) on the 12 GB RTX 3060 — a
**42-second 4-prefill + 3-decode smoke producing 102,400 finite F32
logits per step**. This crosses the threshold from "8-layer ceiling
on Q4_K_M" to "full real-model V2-Lite on a single RTX 3060".

### Commits this round (`8706c15..0fa2852`)

```
0fa2852 CUDA: Q3_K + Q4_1 + Q5_1 dequant — full V2-Lite-Q3_K_M end-to-end
```

### What landed

The cascading discoverability pattern from running the smoke surfaced
three quant types the loader didn't yet handle. Q3_K_M GGUFs use:
- `Q3_K` (11) for the bulk of MoE expert tensors and token_embd
- `Q5_1` (7) for some attention matrices (the q_a / kv_a routing-style
  tensors at high precision)
- `Q4_1` (3) showed up alongside in adjacent quant footprints

**CPU dequant** (`Dequantize.cs` + `DequantizeKQuants.cs`):
- `QuantizationType.Q3_K = 11` enum + `ComputeByteCount` dispatch
- `DequantizeQ3_K` (110-byte super-block: hmask[32] + qs[64] +
  scales[12] + d[2]). The 12 packed bytes decode into 16 unsigned
  6-bit scales (low-nibble + 2 high bits per scale), each becomes a
  signed scale via `scale - 32`. Per-element `value = d ×
  signedScale × (((hbit<<2) | qbits) - 4)`.
- `DequantizeQ4_1Scalar` (20-byte block, `value = d * nibble + m`)
- `DequantizeQ5_1Scalar` (24-byte block, `value = d *
  ((qh_bit<<4) | nibble) + m`)
- All three wired into `RowByteSize` + `ToFloat32` dispatch.

**GPU dequant** (`native/kernels/dequant.cu` + PTX rebuild):
- `dequant_q3_k_f16` — 256 threads/super-block, each thread t (0..255)
  decodes its single element: `sub = t / 16`, looks up its 6-bit
  packed scale, reads its 1-bit hmask + 2-bit qs, writes
  `__float2half(d * signedScale * (((hBit<<2) | qBits) - 4))`.
- `dequant_q4_1_f16` and `dequant_q5_1_f16` mirror the legacy-quant
  pattern (32 elements per thread block).
- `CudaKernels.LaunchDequantToF16` dispatch + module field/wire-up
  for the three new entry points.

**MLA KV-cache patch** for the smoke: V2-Lite's native
`MaxSequenceLength=163840` would need ~37 GB just for the MLA KV
cache (27 layers × 16 heads × (192 qk + 128 v + 64 rope) × F16). The
test patches the config with `MaxSequenceLength = 16` (4 prefill +
3 decode + headroom), which is a smoke-only override — production
sizes the cache to actual prompt+decode budget.

### The cascading discoverability pattern

Each smoke run revealed one more missing piece, and the iterative
fix pattern was:
1. First run: `Unknown quantization type: Q3_K` from
   `QuantizationTypeExtensions.ComputeByteCount` — added that case.
2. Second run: `Unknown quantization type: Q5_1` — added Q5_1 + Q4_1
   to `RowByteSize` + scalar dequant + `ToFloat32` dispatch.
3. Third run: `GPU dequantization not supported for Q5_1` — added
   GPU PTX kernels + dispatch.
4. Fourth run: `CUDA error 2: out of memory` in
   `CudaMlaKvCache..ctor` — patched the test config with
   MaxSequenceLength=16.
5. Fifth run: **passed in 42 seconds**, 16/16 unit tests green.

### Validation

Final regression sweep — 16 tests, 0 failures:
- 3 Q3_K CPU dequant tests (single-block hand-calc, RowByteSize,
  non-aligned-throws)
- 1 GGUF metadata extractor (V2-Lite metadata)
- 4 CudaMoeGroupedGemvTests (Phase B Q4_K parity)
- 3 synthetic CudaTransformerMlaForwardTests (CPU↔CUDA parity)
- 4 Q4_K_M real-GGUF smokes (1/2/4/8-layer)
- **1 Q3_K_M real-GGUF smoke (NEW, 27-layer full V2-Lite)**

Build clean: 0 warnings, 0 errors.

### Open follow-ups (carried forward from Round 12)

- **Q5_K / Q6_K / Q8_0 grouped-GEMV variants** (task #16): the
  Phase B `moe_grouped_gemv.cu` kernel currently covers Q4_K only;
  experts of other K-aligned quant types fall back to the Phase A
  direct GEMV path. A worktree-spawn bug attempted to delegate this
  in parallel with the Q3_K work; agent stopped without doing
  damage. Foreground replacement deferred to next round.
- **Worktree-spawn base mismatch**: agent worktrees were branching
  off public-release `c709a22` (origin/main on `kkokosa/dotLLM`)
  instead of the current feature branch tip (`feb7d76` then
  `0bc0110`). The agent self-detected the mismatch and stopped
  cleanly. Long-term fix: either pre-stage the worktree at the
  intended SHA before spawning, or have the agent template
  fast-forward to a passed-in base SHA on entry.

### Disk state (unchanged from Round 12 modulo the new smoke pass)

- `~/.dotllm/models/bartowski/DeepSeek-Coder-V2-Lite-Instruct-GGUF/`:
  - `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf` (10.4 GB) — works
    at 8 layers max on 12 GB GPU.
  - `DeepSeek-Coder-V2-Lite-Instruct-Q3_K_M.gguf` (8.13 GB) —
    **all 27 layers run on 12 GB GPU**.

## Round 12: Phase B grouped-GEMV kernel + Q3_K loader gap

Two parallel agents this round:

- **Phase B agent** (✅ landed in `0bc0110`) — cross-expert grouped GEMV
  kernel for Q4_K. New `native/kernels/moe_grouped_gemv.cu` walks
  K_active experts per launch instead of one launch per active expert.
  Eligibility: Quantized + seqLen=1 + hidden%256==0 + Q4_K + active>0.
  Down_proj (K=intermediate=1408, NOT 256-aligned) keeps the dequant
  fallback. 4/4 new kernel tests pass; 7/7 existing critical tests
  still green; 4/4 real-GGUF Q4_K_M smokes still green.
- **Q3_K_M-fetch agent** (worktree-spawn bug) — agent's worktree was
  branched off public-release `c709a22` instead of `feb7d76`; correctly
  stopped without doing anything destructive. Foreground replacement
  downloaded the GGUF (8.13 GB) and added the test, but **discovered
  a real loader-side gap**: Q3_K_M GGUFs use GGUF quant type 11 (Q3_K)
  for token_embd.weight, which `GgufReader` doesn't recognize.

### Phase B perf observation

The agent didn't bench decode throughput — that requires KV-cache
wiring across multiple decode steps in a perf harness, separate work.
Structurally, V2-Lite Q4_K_M gate/up across 6 active experts × 26 MoE
layers = 312 projections per decode previously each issued a separate
GEMV launch (~22 µs WDDM each = 6.9 ms launch overhead). Phase B
collapses this to 2 launches per layer (gate-grouped + up-grouped)
× 26 = 52 launches (~1.1 ms overhead). **~5.8 ms/decode saved on
launch overhead alone**, on top of Phase A's bandwidth savings.

### Q3_K loader gap

Adding Q3_K support is a real follow-up (~4-6h focused work):
  * `QuantizationType` enum: add `Q3_K = 11`
  * `Dequantize.RowByteSize`: Q3_K row layout (per ggml-quants.h:
    16 bytes scales + 80 bytes 3-bit quants per 256-element block ⇒
    96 bytes/256 elements vs Q4_K's 144)
  * `Dequantize.ToFloat32`: scalar dequant
  * GPU `dequant_q3_k_f16` kernel (mirrors q4_k pattern)
  * `quantized_gemv_q3_k` for the GEMV fast path (or just dequant
    fallback for v1)

The Q3_K_M GGUF is downloaded and ready; the new
`RealGguf_Q3KM_FullModel_27LayerSmoke` test is in place and gracefully
skips with the Q3_K-not-supported message. Once the loader gap closes,
the test activates.

### Disk state

- `~/.dotllm/models/bartowski/DeepSeek-Coder-V2-Lite-Instruct-GGUF/`:
  - `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf` (10.4 GB) — works,
    loads at 8 layers max on 12 GB GPU.
  - `DeepSeek-Coder-V2-Lite-Instruct-Q3_K_M.gguf` (8.13 GB) — cached,
    won't load until Q3_K dequant lands.

## Round 11: Phase A — direct quantized GEMV in MoE forward

Replaced the dequant→F16→F32→LinearF32 chain in `CudaMoeFfn.ProjectF32OrQuant`
with a direct `LaunchQuantizedGemv` for K-aligned projections (gate_proj
and up_proj at K=hidden=2048, both 8×256). down_proj stays on the
dequant fallback (K=intermediate=1408, NOT 256-aligned — would need a
Q4_K kernel that handles partial trailing blocks; out of scope for now).

Per-projection cost change at V2-Lite Q4_K_M:
  * Before: dequant (M*K F16 scratch materialised, ~5.6 MB write) +
    F16→F32 convert (~11 MB write) + LinearF32 (small mat-vec).
    Three kernels, each doing M*K work.
  * After: F32→F16 input convert (K=2048 elems, ~4 KB) +
    LaunchQuantizedGemv (M=1408 elems output, F16, raw quant streamed) +
    F16→F32 output convert (M=1408 elems, ~5.6 KB). Three kernels, each
    doing K- or M-element work — orders of magnitude less bandwidth.

For the 2/3 of MoE projections that are K-aligned (gate + up across all
6 active experts × 26 MoE layers = 312 projections per decode), the
per-projection bandwidth drops ~10-20×. The down_proj third (104
projections per decode) keeps the original ~5.6 MB scratch path.

Dedicated tiny F16 scratches added to `CudaMoeScratch`:
`_gemvInputF16` (K_max halfs ≈ 4 KB) + `_gemvOutputF16` (M_max halfs ≈
3 KB). Allocated lazily only on the quantized path.

### Validation

7/7 critical tests still green:
  * 3 synthetic CudaTransformerMlaForwardTests (5e-2 vs CPU oracle —
    GEMV path is correctness-equivalent to dequant-then-GEMM).
  * 4 real-GGUF smokes (1/2/4/8-layer V2-Lite Q4_K_M).

Build clean (0 warnings, 0 errors).

### What's NOT changed

The 16-layer GPU OOM ceiling is **unchanged** — Phase A trims per-call
scratch but does NOT shrink the per-expert raw-quant residency
(~975 MB per V2-Lite MoE layer). To fit more layers, one of:
  * Phase B (cross-expert grouped GEMM with raw-quant streaming) —
    documented in `docs/perf/DEEPSEEK_QUANTIZED_GPU_PATH.md`. ~1-2
    days of focused kernel work for the Q4_K variant alone.
  * MoE expert offloading (host mmap + PCIe-paging per active expert) —
    different architecture, probably ~150 ms/decode at PCIe x16 limit.
  * Lower quantization (Q3_K_M) — model-level decision; fits all 27
    layers at ~6.5 GB.

## Round 10: pre-merge sweep — 8-layer ceiling on RTX 3060 + 1527/1549 tests green

Final pre-merge validation. The branch is **ready for review against
parent `feature/mamba-3`**.

### Commits this round (`b0d3f28..6e79c8b`)

```
6e79c8b CUDA: skip F32 host MoE dequant on GPU loads + 4/8-layer real-GGUF smoke
e21a8b8 .continue-here.md: Round 9 — task #10 + #11 complete
```

### What `6e79c8b` fixed

The 2-layer real-GGUF smoke (b0d3f28) was working, but pushing
beyond hit a **CPU host-RAM ceiling** in
`TransformerWeights.LoadDeepSeekMoeLayer`: the F32 dequant of the
3D-stacked MoE expert tensors costs ~2.2 GB host RAM per V2-Lite
Q4_K_M layer. At 4 layers (1 dense + 3 MoE), a 16 GB host runs out
before reaching the GPU upload step.

The F32 dequant exists only for the CPU `MoeSwiGluMlp.Execute` oracle
— it is never read by the GPU loader, which uses the raw GGUF mmap
views directly. So the fix is to skip it on GPU-only loads.

`TransformerWeights.LoadFromGguf` gains a `skipF32MoeDequant` flag
(default `false` to preserve CPU-only callers); plumbed through
`LoadMlaLayer` → `LoadDeepSeekMoeLayer`. When true, per-expert and
shared-expert F32 dequants are zero-filled instead of allocated; raw
views (`GateExpsRaw` etc.) stay populated for the GPU. `CudaTransformerModel.LoadFromGguf`
sets it to `true` since the CPU MoE oracle is never called on a GPU
model.

### The actual on-RTX-3060 ceiling

Empirical layer ceiling found via the new `RealGguf_QuantizedMlaMoe_NLayerSmoke([4, 8])`
theory:

| NumLayers | MoE layers | Result |
|-----------|------------|--------|
| 1 | 0 | ✅ pass (3 s) — Round 8's first smoke |
| 2 | 1 | ✅ pass (5 s) — Round 9's two-layer smoke |
| 4 | 3 | ✅ pass (~3 s) — Round 10 |
| 8 | 7 | ✅ pass (~11 s) — Round 10 ceiling |
| 16 | 15 | ❌ GPU OOM (CUDA error 2) |
| 27 | 26 | ❌ GPU OOM (CUDA error 2) |

**8 layers (~7 GB GPU resident) is the practical ceiling on a 12 GB
RTX 3060** with cleared background processes. Per-MoE-layer GPU
footprint at V2-Lite Q4_K_M: ~975 MB raw quant bytes (64 experts × 3
projections × ~5 MB each + ~10 MB shared MLP + ~5 MB MLA).

The ceiling is closed by **grouped-GEMM compaction** — single GEMM
kernel per layer that walks bucketed (token, expert) assignments
instead of N kernels per active expert. This eliminates the 192
launches/layer/token in `CudaMoeFfn.Forward` AND can hold raw quant
weights in shared on-the-fly memory rather than all-resident.
Estimated 1-2 weeks focused kernel work; **not required for
correctness** — V2-Lite at Q3_K_M (smaller quant) already fits all
27 layers at 12 GB.

### Pre-merge test sweep (Round 10)

- **GPU tests (Category=GPU)**: **194 pass / 0 fail / 13 skipped**
  (skipped on missing model fixtures: HF safetensors, larger Q models).
  Includes both real-GGUF smokes (1-layer + 2-layer + 4-layer +
  8-layer V2-Lite Q4_K_M) and all 3 synthetic MLA + MoE parity tests.
- **CPU tests (Category!=GPU)**: **1333 pass / 0 fail / 9 skipped**
  (skipped on missing fixtures). Includes all GGUF DeepSeek-V2/V3
  config extractor tests and the synthetic + real-checkpoint metadata
  smokes.
- **Build**: 0 warnings, 0 errors across `DotLLM.Models`,
  `DotLLM.Cuda`, `DotLLM.Tests.Unit`.

**Total: 1527 passing / 22 skipped / 0 failing across the entire suite.**

### What ships with this branch

End-to-end DeepSeek-V2-Lite Q4_K_M on CUDA:
- GGUF metadata extraction → `MlaConfig` + `MoeConfig` (validated against
  the real cached 10.4 GB checkpoint)
- CPU GGUF MLA tensor loader (with optional F32-dequant skip for GPU paths)
- CPU GGUF MoE 3D-stacked-expert tensor loader (with optional F32-dequant skip)
- CUDA quantized MLA loader + forward branch (real Q4_K_M weights, dequant-then-HGEMM per projection)
- CUDA quantized MoE loader + forward branch (per-expert raw bytes, dequant→F32-convert→GEMM per active expert)
- Multi-layer prefill on real Q4_K_M GGUF: finite logits, 8 layers fits 12 GB GPU

Plus the preceding Tier 1+2 perf wave (CUDA decode 2.58× on SmolLM,
~3× on Qwen3-8B) is fully retained — none of the GGUF / DeepSeek
work touches the existing GQA / quant GEMV hot paths.

## Round 9: Task #10 + #11 complete — real DeepSeek-V2-Lite-Q4_K_M on GPU

The full GGUF→CUDA→quantized-MLA→quantized-MoE chain now runs end-to-end
on real production weights. Decisive validation:
**`RealGguf_QuantizedMlaMoe_TwoLayerSmoke` passes in 5 seconds** on the
cached 10.4 GB `bartowski/DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf`,
loading layer 0 (dense FFN + MLA, both quantized) plus layer 1 (MoE
FFN with 64 experts + 2 fused shared experts + MLA, all quantized) and
producing 102,400 finite F32 logits.

### Commits this round (`a57eb6a..b0d3f28`)

```
b0d3f28 CUDA: wire quantized MoE forward path + 2-layer real-GGUF smoke (#10-iii + #11)
a936688 CUDA: quantized MoE loader infrastructure (#10-i + #10-ii of task #10)
a57eb6a CUDA: real-GGUF quantized MLA layer-0 smoke test (passes!)
```

### What landed

- **MoeLayerWeights** (CPU side) extended with raw GGUF mmap base
  pointers + quant types + (M, K) dim metadata for the fused-experts
  3D tensors (`GateExpsRaw`, `UpExpsRaw`, `DownExpsRaw`). Single-base-
  pointer-with-offset approach matches the GGUF on-disk layout — avoids
  per-expert 64-entry nint[] allocation. Plus optional shared-expert
  raw views (single fused MLP per DeepSeek convention).
  `HasRawQuantView` discriminator gates the GPU loader's fast path.
- **CudaMoeLayerWeights** (GPU side) extended with `MoePrecision`
  enum (F32, Quantized) + per-projection `QuantType` fields (gate /
  up / down + shared variants).
- **CudaMoeWeightsLoader.LoadLayerQuant** uploads raw quant bytes per
  routed expert (zero-copy from CPU mmap) + per-shared-expert. Memory
  footprint at V2-Lite Q4_K_M: ~26 GB across 26 MoE layers — exceeds
  12 GB GPU; partial loads (NumLayers patching) are the workaround
  until grouped-GEMM compaction lands.
- **CudaMoeFfn.Forward** routes all 6 cuBLAS LinearF32 sites (3 routed
  + 3 shared) through a new `ProjectF32OrQuant` helper. Quantized
  path: `LaunchDequantToF16` → `LaunchConvertF16ToF32` → `LinearF32`.
  +2 launches per projection (cost dominated by cuBLAS GEMM at v1
  scope).
- **CudaMoeScratch** gains F16 + F32 dequant buffers sized to the
  larger of (intermediate × hidden) and (sharedIntermediate × hidden).
  Allocated lazily only on the quantized path.

### Validation

- **`RealGguf_QuantizedMlaMoe_TwoLayerSmoke`** loads the real 10.4 GB
  Q4_K_M GGUF with NumLayers=2, asserts both layer 0 (Mla) and layer 1
  (Mla + Moe) routed through the quantized loader paths via reflection,
  runs prefill on 4 tokens, asserts all 102,400 logits finite. **5
  seconds runtime on RTX 3060.**
- **`RealGguf_QuantizedMla_Layer0Smoke`** (task #9 milestone, kept
  green) — single-layer dense + MLA on real weights.
- **3 synthetic CudaTransformerMlaForwardTests** still green within
  5e-2 vs CPU oracle. F32 MoE path through `ProjectF32OrQuant`
  produces identical results to the previous direct-LinearF32 path.
- **194/204+ GPU tests pass**, 1339+ CPU model tests pass, 35+ GGUF
  config + DeepSeek-V2 tests pass. 0 fail across the board.

### What's left (perf, not correctness)

The full 27-layer real-V2-Lite load OOMs at ~26 GB MoE quant weights
on a 12 GB GPU. The path to fitting full V2-Lite end-to-end is:
1. **Grouped-GEMM compaction** — single GEMM kernel per layer that
   walks bucketed (token, expert) assignments instead of one kernel
   per (active expert, projection). Eliminates the 192 launches/layer/
   token in `CudaMoeFfn.Forward` AND can compact raw quant weights
   into shared on-the-fly memory rather than holding them all
   resident. Estimated 1-2 weeks of focused kernel work.
2. **Per-token dequant scratch reuse** (cheaper, partial fix) —
   collapse the F16 + F32 scratches into a single buffer that's
   recycled per cuBLAS call. Saves ~5.5 MB on hot paths.

Neither is required for correctness — the existing infrastructure
runs the model end-to-end (just not all 27 layers at once on a 12 GB
GPU). Production workloads either: (a) target Q3_K or smaller quants
(fit in 12 GB), (b) use a 24 GB+ GPU, (c) wait for grouped-GEMM.

## Round 8: Task #9 complete — quantized MLA on GPU

Three sub-task commits land the full quantized-MLA path from raw GGUF
mmap bytes through CudaMlaAttention.ForwardF16 with on-device dequant:

```
58ab253 CUDA: wire quantized MLA forward path (#9-iii of task #9)
453557b CUDA: quantized MLA loader infrastructure (#9-i + #9-ii)
9ebba26 GGUF: fix MLA attention.key_length interpretation (total qk_head)
```

### What landed

- **MlaLayerWeights** (CPU side) extended with raw GGUF mmap pointer +
  quant type fields per projection (`QAProjRaw`/`QAProjRawQt`, etc.)
  alongside the existing F32 dequants. `HasRawQuantView` discriminator.
- **CudaMlaLayerWeights** (GPU side) extended with `MlaPrecision.Quantized`
  variant + per-projection `*Quant` + `*QuantType` fields.
- **CudaMlaWeightsLoader.LoadLayerQuant** uploads raw GGUF Q4_K bytes
  zero-copy from CPU mmap → GPU via `RowByteSize`-derived size math.
  ~140 MB of GPU RAM for V2-Lite MLA total (vs ~1.4 GB F16 fully-resident).
- **CudaWeights.LoadFromGguf** routes through the quant loader when
  `MlaLayerWeights.HasRawQuantView` is true (i.e. GGUF source);
  safetensors source still uses the F16 fallback unchanged.
- **CudaMlaScratchF16** gains a `_dequantScratch` buffer sized to the
  largest MLA projection (~4 MB on V2-Lite). Allocated lazily only on
  the quantized path.
- **CudaMlaAttention.ForwardF16** routes all 5 cuBLAS LinearF16 calls
  (q_a, q_b OR q monolithic, kv_a_proj_with_mqa, kv_b, o_proj) through
  a new `ProjectF16OrQuant` helper. Quantized path:
  `LaunchDequantToF16(weight, qt, scratch)` then `LinearF16(input, scratch)`.
  +135 launches/forward at V2-Lite × 27-layer scale ≈ 3 ms WDDM
  overhead — small relative to cuBLAS HGEMM time.

### Validation

- **3/3** existing CudaTransformerMlaForwardTests still green within
  5e-2 vs CPU oracle (the F16 path through ProjectF16OrQuant produces
  bit-identical output to the previous direct-HGEMM path).
- **192/203** GPU tests pass (was 191/202; +1 likely a small fixture
  add). 0 fail, 11 skip on missing model fixtures.
- **1339+ CPU model tests** still green.

### What's next: task #10 (quantized MoE)

Same structural pattern as task #9 but for the FFN side, with the
twist that GGUF MoE tensors are 3D-stacked (`ffn_*_exps.weight` with
shape `[K, M, num_experts]`). Sub-tasks per the design note:

- **#10-i**: CPU `LoadDeepSeekMoeLayer` skips F32 dequant; stores raw
  GGUF tensor pointer + per-expert byte stride + quant type. The
  existing `SliceExpertsToF32` helper is replaced by a "compute slice
  pointer" helper.
- **#10-ii**: CUDA `CudaMoeWeightsLoader.LoadLayerQuant` uploads raw
  3D tensor bytes to GPU as one contiguous buffer. `CudaMoeFfn.Forward`
  branches on precision: per active expert, dequant its slice into
  scratch via `LaunchDequantToF16` and then HGEMM. Sequential v1
  (192 launches/layer/token at V2-Lite scale — likely slow but
  correctness first).
- **#11**: real-checkpoint smoke against the cached
  `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf`. The `DebugMaxLayers=1`
  shortcut tests just layer 0 (dense + MLA, no MoE) once #9 lands —
  full multi-layer needs #10.

GPU memory budget at V2-Lite Q4_K_M: ~140 MB MLA + ~8.6 GB MoE expert
quant bytes + ~260 MB shared experts + ~13 MB router + ~0.5 GB
LM head/embed + ~0.5 GB KV cache ≈ **~10 GB**. Tight but fits 12 GB
RTX 3060 with cleared background processes.

## Round 7: GGUF DeepSeek-V2 fully wired + real-file smoke

After Round 6 landed Phase 1+2 (config extractor + MLA tensor loader),
Round 7 closes the GGUF prerequisite by adding the **MoE 3D-stacked-expert
tensor loader** (Phase 3) and validating against the **real cached
`DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf`** (10.4 GB, downloaded
in-session).

### Commits this round (`d2bbb98..9ebba26`)

```
9ebba26 GGUF: fix MLA attention.key_length interpretation (total qk_head)
c33df93 GGUF: end-to-end DeepSeek-V2 synthetic load integration test
d2bbb98 GGUF: DeepSeek-V2/V3 MoE 3D-stacked-expert tensor loader (Phase 3 of task #12)
```

### What landed

- **`LoadDeepSeekMoeLayer`**: per-layer GGUF reader for the fused-experts
  3D rank-3 tensors `ffn_gate_exps` / `ffn_up_exps` / `ffn_down_exps`
  (shape `[K, M, num_experts]` innermost-first). Each expert's slice is
  dequantized as a contiguous `M*K`-element run into its own 64-byte-aligned
  F32 host buffer (tracked in the loader's owned-allocations list).
  Optional shared experts (`ffn_*_shexp.weight`, DeepSeek's single fused
  shared-MLP convention) loaded inline. Fail-fast shape validation.

- **`SliceExpertsToF32`** helper: byte-offset arithmetic per expert
  (`base + e * (M * RowByteSize(K, qt))`), F32 dequant per slice. White-box
  unit tested with deterministic ramp data + 3-experts × tiny-shape fixture.

- **`LoadMlaLayer` integration**: per-layer FFN dispatch branches on
  `MoeConfig.IsMoeLayer(layerIdx)` and calls `LoadDeepSeekMoeLayer` when
  MoE is active for that index, otherwise the existing dense FFN path.

- **Real-checkpoint smoke**: `RealGguf_ConfigExtractor_ParsesDeepSeekV2Lite`
  opens `bartowski/DeepSeek-Coder-V2-Lite-Instruct-GGUF/...Q4_K_M.gguf`
  if cached and asserts every V2-Lite hyperparameter (HiddenSize=2048,
  NumLayers=27, MLA q_lora_rank=0 / kv_lora=512 / qk_nope=128 /
  qk_rope=64 / v_head=128, MoE 64+2 experts × 1408 intermediate,
  leading_dense=1).

- **`attention.key_length` fix**: the real GGUF revealed
  `attention.key_length=192` (= total qk_head_dim, not qk_nope only).
  The extractor now derives `qk_nope = key_length - rope.dimension_count`,
  matching llama.cpp's gguf_writer convention.

- **Synthetic GGUF fixture**: `DeepSeekV2GgufLoadTests` builds a 2-layer
  V2-Lite-shaped GGUF in-memory (layer 0 dense, layer 1 MoE; F32
  throughout) and round-trips it through `TransformerWeights.LoadFromGguf`.
  Exercises both monolithic-Q (V2-Lite) and LoRA-Q (V2-full / V3) paths.
  237 CPU model tests pass, build clean (0/0).

### What's blocking real DeepSeek-V2-Lite-Q4_K_M end-to-end on CUDA

The CPU GGUF loader works. **The remaining gap is purely GPU memory
pressure**: the F32 host-side dequant of MoE expert weights would need
~57 GB of host RAM (untenable), and a one-shot F16 dequant on GPU at
load would need ~28 GB of GPU RAM (doesn't fit in 12 GB). The viable
path is to keep raw Q4_K bytes on GPU and dequant per-call into reused
F16 scratch — that's tasks #9 + #10.

**Design note for next session**: `docs/perf/DEEPSEEK_QUANTIZED_GPU_PATH.md`
walks through the 5 sub-tasks (~14 hours total focused work) with code
sketches, scratch-buffer sizing, and the alignment risks (V2-Lite MoE
intermediate=1408 isn't a 256 multiple — quantized GEMV needs the
dequant-to-scratch fallback for that path).

## Round 6: GGUF DeepSeek-V2 prerequisite (Phase 1+2 of task #12)

After Round 5 shipped Tier 1+2, parallel-agents 3b + 3c discovered the
real Tier 3 blocker: **the GGUF loader has no DeepSeek-V2 support at
all**. Neither MLA tensors (`attn_q_a` / `attn_kv_*`) nor MoE tensors
(`ffn_*_exps`) nor the relevant config keys are read from GGUF. Both
agents stopped without committing per their stop-and-report budget.

The round-6 commit (`4812927`) lands Phase 1+2 of the prerequisite:

- **GGUF config extractor**: maps `"deepseek2"` → `Architecture.DeepSeekV2`
  and `"deepseek3"` → V3 (was both → legacy `DeepSeek` placeholder which
  the dispatcher refused). Extracts `MlaConfig` from
  `{arch}.attention.{q_lora_rank, kv_lora_rank, key_length, value_length}`
  + `{arch}.rope.dimension_count`. Extracts `MoeConfig` from
  `{arch}.{expert_count, expert_used_count, expert_shared_count,
  expert_feed_forward_length, leading_dense_block_count}`. The leading-
  dense-block-count maps to `MoeConfig.MlpOnlyLayers` so `IsMoeLayer`
  dispatches correctly. YaRN params on `rope.scaling.*` are forwarded
  into `MlaConfig` so `ComputeYarnSoftmaxScaleMultiplier` works.
  `HeadDim` is patched to the full `qk_head_dim` (= `qk_nope` + `qk_rope`)
  for the GQA-shaped pieces of the model that don't know about MLA's
  sub-dims.

- **CPU GGUF MLA tensor loader** (`TransformerWeights.LoadMlaLayer`):
  resolves `attn_q_a`/`attn_q_a_norm`/`attn_q_b` for LoRA-Q, or
  `attn_q` for monolithic-Q V2-Lite; plus `attn_kv_a_mqa` /
  `attn_kv_a_norm` / `attn_kv_b` / `attn_output`. Each tensor is
  Q4_K-dequantized to a 64-byte-aligned F32 host buffer and tracked in
  the loader's owned-allocations list (freed on Dispose). Legacy GQA
  Q/K/V slots are zeroed; the FFN path falls through to the existing
  dense loader for non-MoE layers.

- **Architecture dispatcher unblock**: `TransformerArchitecture.CreateModel`
  routes `DeepSeekV2` / `DeepSeekV3` through `TransformerModel.LoadFromGguf`
  (was: throw "MLA not yet implemented"). Pre-V2 `Architecture.DeepSeek`
  still throws but with a clearer "re-export from V2/V3 checkpoint"
  hint.

Tests: 3 new `GgufModelConfigExtractor` cases (V2-Lite monolithic-Q +
64 routed + 2 shared experts + leading_dense=1; V2-full LoRA-Q;
V3 with 256 experts and leading_dense=3). All 30 GGUF config tests
+ 233 model-loading unit tests pass. Build clean (0/0).

**Phase 3 deferred to task #13**: MoE 3D-stacked-expert tensor loader
(`ffn_gate_exps` / `ffn_up_exps` / `ffn_down_exps` shape
`[experts, intermediate, hidden]`). Until that lands, MoE-band layers
in a real V2-Lite GGUF will trip an `InvalidDataException` pointing
back to the loader. Estimated 4-6h focused work for Phase 3 — needs
slicing logic + per-slice Q4_K dequant. Once Phase 3 lands, real
`DeepSeek-V2-Lite-Q4_K_M.gguf` can load end-to-end (modulo host RAM
pressure from F32 dequant — that's the on-device-dequant task 9/10
follow-up).

## Round 5: rebase + MLA/MoE dispatch glue (Tier 1 + Tier 2)

Parent `feature/mamba-3` advanced 45 commits (mostly Vulkan MLA port + CPU
perf 2.7× prefill + decode-thread-cap). **Rebased cleanly with `-X theirs`**
(only `.continue-here.md` was a textual conflict — resolved by always taking
the newer revision). 0-warning, 0-error build on both sides of the rebase.
Origin/feature/mamba-3-cuda is now **stale at the pre-rebase tip 7cc7b46** —
needs force-push-with-lease.

### Commits this round (`5307713..3f2d746`)

```
3f2d746 CUDA: parameterize graph-equivalence test + rollback/graph guard
5a5c405 CUDA: tiny-random DeepSeek-V2 forward parity tests + FfnNorm dtype fix
ee4745f CUDA: dispatch MLA + MoE through CudaTransformerModel.Forward
5307713 CUDA: MLA + MoE weight loading in CudaWeights
```

(Plus the 57 rebased pre-rebase commits — same content, new SHAs.)

### Tier 1 — MLA + MoE end-to-end dispatch

`CudaTransformerModel.Forward` now branches on `Config.MlaConfig` /
`Config.Moe` and routes through `CudaMlaAttention.ForwardF16` (DeepSeek-V2/V3
attention) / `CudaMoeFfn.Forward` (Mixtral / Qwen-MoE / DeepSeek-MoE FFN)
instead of the GQA + dense FFN paths.

- **`CudaWeights` extension** (`5307713`): two new side-tables —
  `MlaLayers` (per-layer `CudaMlaLayerWeights`, populated when
  `MlaConfig != null`) and `MoeLayers` (per-layer nullable
  `CudaMoeLayerWeights`, populated for routed-MoE layers). GQA Q/K/V/O
  uploads gated on `lw.Mla is null`; dense gate/up/down uploads gated on
  `lw.Moe is null`. AttnNorm/FfnNorm uploads stay shared. New
  `CudaMoeWeightsLoader` mirrors the existing `CudaMlaWeightsLoader`
  pattern (F32 router + per-routed-expert + per-shared-expert + optional
  sigmoid gate).

- **`CudaTransformerModel.Forward` dispatch** (`ee4745f`): per-model
  MLA/MoE state lazily allocated on first invocation that needs it
  (FP16 MLA scratch, F16 MLA KV cache, F32 MoE staging buffers for the
  F16↔F32 conversion at the FFN boundary, F32 RoPE cos/sin tables sized
  for `Config.MaxSequenceLength × QkRopeHeadDim/2`). Per-layer logic:

  ```
  Attention block:
    if (isMla)  CudaMlaAttention.ForwardF16(Residual, NormOutput, ...)
                FusedAddRmsNorm(Residual, NormOutput, lw.FfnNormWeight, NormOutput)
                goto FfnBlock
    else       (existing GQA path unchanged)

  Ffn block (per-layer):
    if (moeLayer)  ConvertF16→F32(NormOutput → moeIn)
                   CudaMoeFfn.Forward(moeIn → moeOut)
                   ConvertF32→F16(moeOut → NormOutput)
                   goto EndOfLayer
    else           (existing dense FFN path unchanged)

  End-of-iter:
    if (isMla)  Add(Residual, NormOutput, Residual or HiddenState)  // raw add — next layer expects raw
    else        FusedAddRmsNorm with next layer's AttnNorm  // existing pattern
  ```

  Layer-0 prep skips the GQA pre-RmsNorm when MLA is active (MLA's
  `ForwardF16` does its input RMSNorm internally). Graph-capture
  early-return now also gates on `!isMla && !isMoe` — both paths are
  eager-only today (MoE has host-side bucketing; MLA absorbed kernel
  uses dynamic shmem).

- **Bug found by the integration test** (`5a5c405`): the MLA path's
  FfnNorm step was passing `mlaLayer.FfnNormWeight` (uploaded F32 by
  `CudaMlaWeightsLoader.LoadLayerF16` because the in-kernel RMSNorm
  takes F32) to `LaunchFusedAddRmsNorm`, which expects an F16 weight
  pointer. First test produced 8/8 NaN logits. Fixed to use
  `lw.FfnNormWeight` from the regular `CudaLayerWeights` slot
  (uploaded F16 by `UploadNormWeight`, sharing the same source array).

- **Tiny-random integration test** (`5a5c405`,
  `tests/.../CudaTransformerMlaForwardTests.cs`): three variants —
  MLA + dense FFN with LoRA-Q (DeepSeek-V2 / V3 shape), MLA + dense
  FFN with monolithic-Q (V2-Lite shape), MLA + alternating-MoE (V2 +
  first-layer-dense via `MlpOnlyLayers=[0]`). All match CPU oracle
  within 5e-2 on RTX 3060. Uses `SafetensorsFixtureBuilder` to write
  HF DeepSeek-V2-style synthetic checkpoints, loads through
  `TransformerModel.LoadFromSafetensors` (CPU) and
  `CudaTransformerModel.LoadFromSafetensors` (CUDA), compares
  last-position prefill logits. **Decisive evidence** the dispatch
  glue actually works end-to-end — kernel-level helpers were already
  green via `CudaMlaForwardTests` + `CudaMoeFfnTests` but model-level
  wiring was unproven.

### Tier 2 — graph parity + defensive guards

- **Graph-equivalence test parameterised** (`3f2d746`):
  `EagerVsGraphDecode_Q4KM_Match` → `EagerVsGraphDecode_Match` with
  `[Theory]` over Q4_K_M and Q8_0 SmolLM-135M variants. Q5_K / Q6_K
  aren't separate model fixtures but Q6_K is exercised implicitly via
  the Q4_K_M model's LM head (`output.weight` is Q6_K).

- **Rollback + graph guard** (`3f2d746`): `CudaKvCache.WasRolledBack`
  flag set on first `Rollback` call. The graph-replay fast path in
  `CudaTransformerModel.Forward` consults it and throws
  `InvalidOperationException` with a clear remediation hint if hit.
  Today speculative decoding is the only `Rollback` caller and is
  restricted to greedy (non-graph), so this guard exists to fail fast
  when a future spec+graph combination wires both on simultaneously.

### What landed end-to-end

**DeepSeek-V2/V3 forward on CUDA now runs** for synthetic FP32
weights → uploaded as FP16 via `LoadFromSafetensors`. CPU↔CUDA parity
within 5e-2 on the tiny-random fixture. **Real DeepSeek-V2-Lite
Q4_K_M still won't load end-to-end** — it requires quantized MLA
weight paths (Tier 3b) and quantized MoE weight paths (Tier 3c),
which are explicit follow-ups.

### What's still open

- **Tier 3b**: Quantized MLA weight paths (Q4_K/Q8_0). Today
  `CudaMlaWeightsLoader.LoadLayerF16` only handles F16 conversion of
  F32 source. Need GEMV variants for q_a/q_b/kv_a/kv_b/o projections
  reading raw quantized bytes. ~1 week.
- **Tier 3c**: Quantized MoE + per-expert quantized GEMV. Today
  `CudaMoeWeightsLoader.LoadLayer` is F32-only. Real DeepSeek-V2-Lite
  has 64 routed experts × Q4_K_M weights. Need quantized projections;
  grouped-GEMM batching across active experts is a stretch goal. ~1-2
  weeks.
- **Real DeepSeek-V2-Lite Q4_K_M end-to-end smoke test** — gates on
  3b+3c.
- **Tier 3a (Phase C hybrid)** — explicit deferral; Phase B alone
  already gets 7×–14× cache savings.
- **CUDA HF-parity gates** — different machine; safetensors fixtures
  not local.

## CUDA commits this session (`a8340b7..7cc7b46`)

```
7cc7b46 .continue-here.md: backfill commits list + suggested-actions for round 4
bc3cb84 .continue-here.md: parallel-agent round 4 — MoE + Phase B + FP16 + GPU mutex
c0fbe30 Merge cuda-mla-fp16: FP16 attention + helpers + dispatch + cache + weights
95264a0 Merge cuda-mla-phase-b: latent KV cache + W_UK absorbed attention
1028a9d CUDA MoE: F32 forward helper + synthetic equivalence tests
471c7e2 CUDA MoE: per-layer weights bundle (CudaMoeLayerWeights)
73f0242 CUDA MoE: F32 helper kernels (softmax-topk, axpy, gather, sigmoid-gate)
0a49dce scripts: GPU access mutex (gpu-lock.sh) for parallel sub-agents
de1cd32 CUDA MLA Phase 1: full F32 forward helper + end-to-end tests
42d171e CUDA MLA Phase 1: kernel + KV cache + weights struct + synthetic tests
69b7022 docs: research note on tensor-core (mma) batched MMQ design space
aed191f CUDA MMQ: dynamic shmem sizing replaces compile-time MMQ_MAX_CHUNKS
4edadc4 CUDA: graph capture default-ON for all k
fc79371 CUDA tests: pre-Q8_1 + `_preq` GEMV equivalence vs on-the-fly Stage 1
1ebae6f CUDA: wire pre-Q8_1 scratch + flip MMVQ-large default ON for k>=1024
7c47493 CUDA: pre-Q8_1 input quant kernel + `_preq` MMQ/MMVQ-large variants
7bca5e7 Tests: loosen EagerVsGraphDecode_Q4KM_Match logit-diff tolerance
f9b8294 CUDA: MMVQ-large 1-row-per-block Q4_K/Q5_K/Q6_K GEMV kernels (opt-in)
        + MMQ_MAX_CHUNKS 256→384 (silent shmem overflow correctness fix on Qwen3-8B MlpDown)
2446ae7 CUDA: route token embedding through per-row K-quant kernels at load
e9f28c0 CUDA: per-row Q4_K/Q5_K/Q6_K embedding lookup kernels
2fa9eaa docs: research note on MlpUp Q4_K GEMV gap vs llama.cpp
a247909 .continue-here.md: capture Qwen3-8B running + weight dedup + perf observations
15becbf CUDA: dedup quantized Q/K/V and Gate/Up weights into the packed buffer
ff61337 .continue-here.md: MMQ Q5_K/Q6_K + correct stale MLA Phase-0 claims
d0004c2 CUDA: extend MMQ dp4a path to Q5_K and Q6_K (graph 206 → 217 tok/s)
9433c89 .continue-here.md: MMQ Q4_K + Norm rewrite + MLA Phase 0 landed (NB: this entry was wrong about Phase 0 — see correction below)
facbcd7 CUDA Norm: vectorized half2/float2, __shfl_xor reduction, fused-add caches FP32 sum in shmem
6368950 CUDA: MMQ-style fused dequant+matmul Q4_K GEMV (dp4a)
57a7ce8 .continue-here.md: KvUpdate fusion + Quantized-KV graph landed
b354700 CUDA: wire fused RoPE+KV-write into eager decode
af8b2b4 CUDA: fused decode-step RoPE + KV-cache write kernel
7edb993 CUDA: tests + bench coverage for quantized KV graph capture
af22540 CUDA: graph-capture-compatible quantized KV cache
bb9d9a9 .continue-here.md: CUDA decode 87 → 188 tok/s session pickup
ce83ce5 CUDA: use fused Q/K/V and Gate/Up GEMV on the decode path
c5fedf8 CUDA: pack Q/K/V and Gate/Up quantized weights for fused decode GEMV
d941329 CUDA: profile-cuda-decode --compare and graph/eager equivalence test
dd04797 CUDA: capture decode forward into a CUDA Graph, replay per token
c591daf CUDA: decode-step device-resident seq_kv / write-pos scaffolding
130ea21 CUDA: per-category decode profiler (cuEvent-based)
55b2a65 CUDA 13: resolve cuBLAS from bin\x64 (subfolder relocation)
```

> **Note on `6ced5b1`** (the "MLA Phase 0" commit referenced in earlier session
> notes): that commit is **not** an ancestor of HEAD on this branch — it was
> authored in some other worktree and never landed. So `docs/CUDA_MLA.md` and
> the MlaConfig expansion via `6ced5b1` are NOT in the working tree. The full
> `MlaConfig` DeepSeek-V2/V3 contract is, however, present on the branch
> (inherited from parent via `e7b4aef` and prior MLA Phase A/B/C commits).

### MMQ Q4_K dp4a kernel (`6368950`) — graph 188 → 200 tok/s

`native/kernels/quantized_gemv_mmq.cu`. Input activation quantized to INT8
per 32-element chunk (matches Q4_K sub-block layout); per-row dot product
via `__dp4a` (packed 4×INT8 multiply-add, sm_61+) instead of FP fmuladd.
Q4_K's per-sub-block min folded as `dmin * m_s * Σxq * dx_c` — single fmaduladd
per sub-block. Tile parallelism: 4 output rows per CUDA block, 256 threads
shared across them via shared-memory accumulators (avoids the "254/256 threads
idle" failure mode at SmolLM's 2-superblock-per-row regime).

Best per-category win: **MlpDown −21 % (1.901 → 1.501 ms)** because its
inputDim=1536 = 6 super-blocks/row (vs 2 for the projections) — more work
per row → MMQ amortization kicks in. QkvProj/MlpUp/OProj see smaller wins
(−3 to −7 %). LmHead unaffected (Q6_K weight, MMQ Q4_K-only for now).

`HasMmqQ4K` accessor + `LaunchQuantizedGemvMmq` launcher; `Project()` and
the fused QKV/GateUp paths route Q4_K decode through MMQ when available.
`DOTLLM_DISABLE_MMQ_Q4K=1` env var forces legacy kernel for A/B comparison.

Logits stay bit-identical to legacy kernel within `Tight` tolerance — input
INT8 requantization noise is dominated by existing Q4_K_M weight-quant noise
budget.

### Norm kernel rewrite (`facbcd7`) — kernel-level cleanup

`rmsnorm_f16` / `fused_add_rmsnorm_f16` / `rmsnorm_f32` / `rmsnorm_f32in_f16out`
/ `per_head_rmsnorm_f16` rewritten with vectorized `half2`/`float2` loads
+ `__shfl_xor_sync` symmetric warp reduction + folded-`1/n` rsqrt arg.
`fused_add_rmsnorm_f16` extra trick: caches FP32 sum in dynamic shared
memory between pass 1 (residual + x) and pass 2 (scale by rms_inv). Pass 2
reads un-truncated FP32 sum from shmem instead of re-reading freshly-stored
FP16 residual — saves one global read + FP16↔FP32 conversion per element.

Per-call kernel speedup is real but small (~1-3 µs each). End-to-end
contribution to graph tok/s is below noise floor at SmolLM scale; will
matter more on larger hidden sizes (Llama-7B+). Adds 4 new kernel
equivalence tests (`RmsNormF16_*`, `FusedAddRmsNormF16_*`).

### Parallel-agent round 4: MoE FFN + MLA Phase B + MLA FP16 (3 agents, GPU mutex)

This round introduced `scripts/gpu-lock.sh` (file-based GPU mutex) so 3
GPU-touching agents could run in parallel without `CUDA_ERROR_ILLEGAL_ADDRESS`
collisions. Pattern: agents acquire the lock around PTX builds + `dotnet test`
+ benchmarks; non-GPU work (code, design, docs) stays parallel. Lock observed
holding ~7 min waits when contended; was the right trade vs the previous
"per-agent retry-with-30s-sleep" pattern.

**Three branches landed in one round** — merged sequentially (MoE first as
independent, then Phase B, then FP16 with hand-spliced reconciliation):

1. **MoE FFN port** (`73f0242 + 471c7e2 + 1028a9d`) — `CudaMoeFfn.Forward`
   helper + 6 tests. F32 throughout, mirrors the just-landed `CudaMlaAttention`
   pattern. Routing softmax+top-k, sequential per-expert SwiGLU, shared-expert
   summation. Synthetic equivalence vs CPU `MoeSwiGluMlp.ExecuteWithSharedExpert`
   passes (1e-3 small / 2e-2 V2-Lite-shape — same Tensor-Core-SGEMM drift seen
   in MLA tests). Reduced V2-Lite test from 64 → 16 routed experts (full 64
   OOMs the per-method recycled CUDA context). **Concrete unblock for
   end-to-end DeepSeek-on-CUDA**: only the dispatch-glue commit and an
   integration test remain before forward runs.
2. **MLA Phase B** (`95264a0`) — latent KV cache + W_UK absorbed attention.
   New `attention_mla_latent.cu` (3 kernels: absorbed attention + Q-absorb +
   V-expand) + `CudaMlaLatentKvCache` + `ForwardLatent` helper + 6 tests.
   **Cache-size reduction confirmed: 7.22× on V2-Lite shapes** (16 heads).
   On V2-full (128 heads) the ratio grows to ~14×. Phase A code untouched.
3. **MLA FP16** (`c0fbe30`) — FP16 sibling kernels for attention_mla.cu and
   mla_helpers.cu, FP16 KV cache option, FP16 weights upload, `ForwardF16`
   dispatch + 10 tests. **F16 cache exactly 0.5× F32** (measured). Stacked
   with Phase B: net Qwen-class KV cache footprint ~120 MiB (was ~1.75 GiB
   pre-Phase-B-pre-FP16). F32-vs-F16 parity within 5e-2 (small) / 0.4
   (V2-Lite synthetic) — V2-Lite tolerance is loose because random
   unit-scale weights amplify FP16 noise; real DeepSeek weights with
   normalised magnitudes would drift far less.

GPU test count: **78 → 100** (+22 across the three agents, 0 fail, 10 skip).

**Merge methodology lesson**: git's 3-way merge produces spurious
"additive-conflicts" when two branches insert at the same line anchor in a
shared file (CudaKernels.cs and CudaMlaAttention.cs hit this 5+10 + 8+10
times). For pure-additive cases the resolution is mechanical (keep both),
but git's diff sometimes intermingles "shared trailer" content from the
end of cuLaunchKernel calls, which a naive marker-strip breaks. The clean
strategy used here: take the file from one branch as the baseline, then
hand-apply specific Edit hunks for the other branch's additions. Captured
~30 min per shared-file merge.

### CUDA MLA Phase 1 (`42d171e + de1cd32`) — Phase A naive forward implemented + tested

CUDA port of `MlaAttention.Execute` (Phase A naive expanded — the CPU
oracle on this branch). Lands the full attention kernel + KV cache +
weights struct + per-step helpers + 9 new tests, validated against the
CPU oracle within FP16 noise (1e-3 to 2e-2 tol depending on shape).

What's in:
- `native/kernels/attention_mla.cu` — Phase A naive online-softmax F32
  attention kernel.
- `native/kernels/mla_helpers.cu` — split, RoPE-on-q-pe-half, RoPE-on-k-pe,
  F32 RMSNorm.
- `src/DotLLM.Cuda/CudaMlaWeights.cs` — `CudaMlaLayerWeights` public
  struct + `CudaMlaWeightsLoader` (uploads MLA tensors as F32 device
  buffers — q_a_proj/q_b_proj/q_a_layernorm, kv_a_proj_with_mqa/kv_b_proj/
  kv_a_layernorm, o_proj).
- `src/DotLLM.Cuda/CudaMlaKvCache.cs` — Phase A expanded layout, F32,
  per-layer K_nope + V + K_pe.
- `src/DotLLM.Cuda/CudaMlaAttention.cs` — `Forward` end-to-end helper
  (norm → Q LoRA/mono → KV LoRA → split+RoPE → cache write → attention
  → o_proj) + `CudaMlaScratch` reusable scratch.
- `src/DotLLM.Cuda/CudaGemm.cs` — `LinearF32` / `GemvF32` cuBLAS wrappers
  for the F32 matmul path.
- 5 kernel-level tests + 4 end-to-end forward tests (all green).

What's deferred (separate follow-up agents):
- **Wiring `CudaMlaAttention.Forward` into `CudaTransformerModel.Forward`**.
  The dispatch glue is the obvious next step, but it blocks on **CUDA MoE
  FFN port** which doesn't exist either. DeepSeek-V2/V3 layers are MLA
  attention + MoE FFN; without the FFN GPU path, end-to-end forward can't
  run on real DeepSeek weights even with MLA attention done.
- Tiny-random DeepSeek-V2 integration test using the new helper.
- Phase B (latent KV cache + W_UK absorption — decode efficiency).
- Phase C (vLLM-style hybrid: prefill expand + decode absorbed).
- FP16 / quantized weight paths (everything is F32 today). FP16 is
  natural follow-up; quantized requires extending `Project` patterns from
  the GQA path.

**Net status**: the kernel-level CUDA MLA primitives are landed and validated.
The remaining DeepSeek-on-CUDA blockers are now (1) MoE FFN GPU port, (2)
dispatch wiring in the CUDA forward pass, (3) Phases B/C for decode efficiency.

### Graph capture default-ON (`4edadc4`) — opt-in → opt-out

`UseGraphCapture` now defaults to `true` on `CudaTransformerModel` when
the kernel set supports it (silently falls back to eager otherwise).
`DOTLLM_DISABLE_GRAPH_CAPTURE=1` env var forces eager. The `--compare`
bench runner explicitly opts the eager pass into eager so the comparison
still works. Existing eager-vs-graph equivalence tests now set
`UseGraphCapture = false` explicitly on the eager construction.

Per the cross-model bench: graph never hurts post-pre-Q8_1, sometimes
helps significantly (SmolLM 2.07× from launch-overhead amortization).
Default-ON is a free win at the small-k end and a no-op at large-k.

### Dynamic MMQ shmem (`aed191f`) — future-proofs Llama-70B+ class

The compile-time `MMQ_MAX_CHUNKS=384` (k≤12288) is replaced with `extern
__shared__ uint8_t s_dyn[]` in all 6 on-the-fly MMQ + MMVQ-large kernels;
the host launcher computes per-call shmem (`numChunks * (36 or 38)` bytes)
and passes it to `cuLaunchKernel`. On module load, `cuFuncSetAttribute(
MAX_DYNAMIC_SHARED_SIZE_BYTES, ...)` opts each kernel into the device's
optin shmem cap (100 KB on sm_86). `_preq` variants are untouched
(already shmem-free).

New synthetic test `MmvqLargeQ4K_MatchesLegacy_Llama70B_Synthetic` at
n=4096, k=14336 (Llama-70B intermediate) passes — the limit is now
device-cap-bound: k ≈ 53000 on sm_86, comfortably covers Llama-405B
intermediate (53248). `CheckDynamicSharedBudget` host-side guard fails
fast above the cap with a clear error.

Perf unchanged on all 3 models (within run-to-run noise).

### Pre-Q8_1 input quant + MMVQ-large default ON (`7c47493 + 1ebae6f + fc79371`) — Qwen3-8B 11.5 → 30.9 tok/s (2.69×)

**The breakthrough commit of the session.** Implements research note §H4
(`docs/perf/MLPUP_GEMV_GAP.md`): a single `quantize_x_to_q8_1` kernel
quantizes the activation `x[k]` to INT8 + per-32-element FP16 scale +
per-half-chunk sums **once per fused-GEMV-input** (not once per CUDA block).
Then `_preq` variants of all MMQ + MMVQ-large kernels skip Stage 1 and
read from the precomputed scratch.

This unblocks the MMVQ-large path that previously regressed 2× without
the prerequisite: MMVQ-large is 1-row-per-block, so without pre-quant the
Stage 1 input-quantization runs `n` times per GEMV (vs `n/4` for MMQ-4-rows).
Pre-quant makes it 1 time, no matter the kernel topology.

**Wiring:**
- `CudaForwardState.PreQ8_1Scratch` — single scratch buffer sized for
  `max(hidden, intermediate)` elements + per-chunk metadata. ~14.6 KB on
  Qwen3-8B.
- `LaunchQuantizeXToQ8_1` runs in `MaybePreQuantize` helper before each
  fused QKV/GateUp/Down/O/LmHead GEMV. ~145 extra launches per forward
  on Qwen3-8B (4/layer × 36 + 1 LmHead) = ~3.2 ms WDDM overhead, dwarfed
  by the GEMV savings.
- `LaunchQuantizedGemvMmq` overload accepts the scratch pointer; routes
  to `_preq` kernel variant. **MMVQ-large is now default ON for k≥1024**;
  the previous `DOTLLM_ENABLE_MMVQ_LARGE_*` env vars are replaced by
  `DOTLLM_DISABLE_PREQ8_1` (forces on-the-fly path) and the existing
  `DOTLLM_DISABLE_MMVQ_LARGE_*` (forces MMQ-4-rows even with pre-quant).
- Pre-quant is gated on `inputDim ≥ 1024` to keep SmolLM (k=576) on the
  unchanged code path — no perf delta at small scale.

**Per-category breakdown on Qwen3-8B Q4_K_M (RTX 3060, eager):**

| Category | Pre (ms) | Post (ms) | Δ |
|---|---|---|---|
| MlpUp (gate+up) | 35.3 | **12.5** | **-64.6%** |
| MlpDown | 23.0 | **6.1** | **-73.5%** |
| QkvProj | 9.7 | **3.7** | **-61.8%** |
| LmHead | 7.9 | **2.1** | **-73.2%** |
| OProj | 6.2 | **2.5** | **-59.6%** |
| Attention | 1.6 | 2.3 | +44% (noise — KV cache state) |
| Total wall | 87.0 | **32.4** | **-62.7%** |
| tok/s | 11.5 | **30.9** | **+169%** |

The bigger the GEMV's `n` (output rows), the bigger the win, because
pre-quant amortizes Stage 1 across more rows. LmHead has n=151936 →
biggest absolute speedup percentage.

**Validation:** 9 new equivalence tests (`PreqQ{4,5,6}K_MatchesOnTheFly` ×
3 shapes) confirm `_preq` variants produce bit-near-identical output to
on-the-fly variants. Control test (`DOTLLM_DISABLE_PREQ8_1=1` +
`DOTLLM_DISABLE_MMVQ_LARGE_*=1`) reproduces the on-the-fly MMQ-4-rows
baseline within noise — isolating the win to pre-Q8_1 + MMVQ-large.

**Where dotLLM lands now**: 30.9 tok/s on Qwen3-8B Q4_K_M is **inside
llama.cpp's reported 25-42 tok/s range** on RTX 3060. Decode is now
competitive at this size; the gap is narrowed from ~3× to within noise
of the reference impl.

### MMQ_MAX_CHUNKS bug fix (in `f9b8294`) — silent correctness fix on k≥8192 GEMVs

The `MMQ_MAX_CHUNKS=256` constant in `quantized_gemv_mmq.cu` capped the
per-block shared-memory `s_xq[256*32]` buffer at k=8192 (256 chunks of 32
elements). On Qwen3-8B, `MlpDown` is `[hidden=4096 × intermediate=12288]`
GEMV — k=12288 → **384 chunks**. The kernel's Stage 1 input-quantization
loop wrote past the `s_xq` buffer (chunks 256..383 trashed adjacent shmem
or simply weren't stored), and Stage 2 read uninitialized values for
those super-blocks. Output was visibly fast (no segfault — shmem
overruns are silent) but mathematically wrong: roughly 1/3 of MlpDown
contributions came from garbage. Generated tokens looked plausible-shaped
but the per-token logits were skewed.

The pre-fix Qwen3-8B "baseline" of 10.5 tok/s was on this garbage. The
corrected baseline (MMQ_MAX_CHUNKS bumped to 384) is **11.5 tok/s** with
correct logits. SmolLM-135M (k=576, 18 chunks) was unaffected.

This is a real regression risk for anyone targeting larger-k models on
this branch's history before commit `f9b8294`. The fix is permanent;
we'll bump again to support k≥12288 with margin if Llama-70B-class arrives.

### MMVQ-large opt-in kernels (in `f9b8294`) — structurally correct, regress today

Implements the 1-row-per-block, 128-thread MMVQ kernel for Q4_K/Q5_K/Q6_K
recommended by the research note (`docs/perf/MLPUP_GEMV_GAP.md`). On
Qwen3-8B with MMVQ-large enabled (`DOTLLM_ENABLE_MMVQ_LARGE_*=1`):
**MlpUp 79 ms/token vs default 35 ms — a 2× regression.**

Why: dotLLM does Stage 1 (input quantization to INT8) **on-the-fly inside
every block**. Going from 4-rows-per-block to 1-row-per-block multiplies
Stage 1 work 4× (the input is the same; we re-quantize it 4× across more
blocks). llama.cpp avoids this with a separate pre-Q8_1 quantization
kernel that runs once per forward and is shared across all GEMVs in the
layer (their §H4 in the research note, deferred). Without that
prerequisite, the structural change alone regresses.

**Decision: kernels stay in tree, gated by opt-in `DOTLLM_ENABLE_MMVQ_LARGE_<QT>=1`
(default OFF).** Disable knobs `DOTLLM_DISABLE_MMVQ_LARGE_<QT>=1` take
precedence for forced A/B testing. When pre-quantization (§H4) lands,
flipping the default ON should unlock the 2-4× win the research projected.

9 new equivalence tests in `CudaMmqKernelTests` cover Q4_K/Q5_K/Q6_K at
Qwen3-8B-class shapes (n∈{4096, 11008, 24576}, k=4096) — all pass with
0.5-1% peak-relative drift, same noise budget as the existing MMQ tests.

### Per-row K-quant embedding lookup (`e9f28c0` + `2446ae7`) — saves 1.16 GiB on Qwen3-8B

Adds CUDA kernels `embedding_lookup_q4_k_f16`, `_q5_k_f16`, `_q6_k_f16`
that match llama.cpp's `get_rows_q*_K` pattern: one block per token,
256 threads each emitting one element of each super-block in the row.
Per-superblock arithmetic is bit-identical to the existing
`dequant_q{4,5,6}_k_f16` kernels (3 new equivalence tests prove it).

Wired into `CudaWeights.LoadFromGguf`: when token-embed quant type is
Q4_K/Q5_K/Q6_K and `hidden % 256 == 0`, **skip the load-time FP16 dequant
of the entire vocab×hidden table** and use the per-row lookup at forward
time instead. Falls back to bulk dequant when hidden isn't a 256
multiple, or when `DOTLLM_DISABLE_EMBED_ROWLOOKUP=1`.

VRAM impact (measured on Qwen3-8B Q4_K_M, vocab=151936, hidden=4096):
- Bulk-dequant (`DOTLLM_DISABLE_EMBED_ROWLOOKUP=1`): 7266 MiB resident
- Per-row lookup: **6078 MiB resident → 1188 MiB / 1.16 GiB saved**

End-to-end perf cost: <1 µs/token (the Embed category was 0.7% of GPU
time, stays at ~0.6% post-change). Standard practice in inference
engines (llama.cpp does this for all K-quants).

### Weight dedup vs packed buffer (`15becbf`) — saves ~2.5 GB on 8B-class models

Replaced the "7 separate quantized uploads + DtoD-pack" pattern in
`CudaWeights.LoadFromGguf` with a try-pack-first approach: when QKV (or
Gate/Up) share quant type + input dim and a GEMV kernel exists, allocate
ONE packed buffer and upload Q/K/V directly into it via H2D copies at row
offsets. The per-tensor `qQuant`/`kQuant`/`vQuant` device pointers become
slices into the packed buffer — bit-identical layout, zero data copy. Only
the packed allocation is in the lifetime tracker. Falls back to per-tensor
uploads when packing isn't possible. O and Down stay per-tensor (no pack
to dedup against).

Two diagnostic helpers added: `AllocOrThrowWithContext` and
`MemcpyHtoDOrThrowWithContext` so OOM failures report the requested size
+ free/total VRAM at the failure moment.

Resident-VRAM impact:
- SmolLM-135M Q4_K_M: ~30 MB saved (negligible at this scale).
- Qwen3-4B Q4_K_M: ~1.4 GB saved.
- Qwen3-8B Q4_K_M: **~2.5 GB saved** (was OOMing at ~2.5 GiB resident
  during weight upload; now lands at ~5.3 GiB resident and runs).

End-to-end perf: SmolLM-135M Q4_K_M unchanged (eager 117 → 111 within
noise, graph 217 unchanged). Bit-identical logits — 47/47 GPU tests pass.

### MMQ Q5_K + Q6_K (`d0004c2`) — graph 206 → 217 tok/s (+5.4%)

Extends the dp4a MMQ pattern from Q4_K to Q5_K and Q6_K in
`native/kernels/quantized_gemv_mmq.cu`. Q5_K reuses the same 32-element
chunk granularity as Q4_K (sub-blocks are also 32 elements); weight bytes
packed in input memory order [lo_e, hi_e, lo_o, hi_o] to match the per-chunk
INT8-quantized x stream. Q6_K sub-blocks are 16 elements so each 32-element
input chunk holds two sub-blocks with separate scales — Stage 1 input-quant
pass tracks per-half-chunk sums (sx_a, sx_b) by stopping the warp reduction
at offset=8 (lanes 0..15 hold one half, lanes 16..31 the other). Q6_K has
no dmin term — math simpler than Q4_K/Q5_K.

Wiring: `HasMmqQ5K` / `HasMmqQ6K` accessors + `DOTLLM_DISABLE_MMQ_Q5K` /
`Q6K` env vars; new `HasMmq(QuantizationType)` helper used by `Project()`
and the fused QKV / Gate-Up paths to route any MMQ-supported type through
the dp4a kernel. `CudaModule.TryGetFunction` so a stale PTX (Q4_K-only MMQ)
loads gracefully rather than crashing startup.

End-to-end win on SmolLM-135M Q4_K_M: graph 206 → 217 tok/s. **LmHead is
Q6_K** in this model (`output.weight` block) so MMQ Q6_K activates and
produces the bulk of the 5.4% lift. Eager (clean) within noise (117.9 →
116.7) — the existing FP-fmuladd path was already cuBLAS-fast for Q6_K at
n=49152 row-parallel scale.

Validation: 6 new `CudaMmqKernelTests` (Q5_K + Q6_K × {(4,256),(8,512),
(64,1024)}) — synthetic random-byte weights vs the legacy `quantized_gemv`
kernels; peak-relative drift 0.4–1.9% across all cases (consistent with the
INT8 input requantization noise budget, same as Q4_K MMQ). End-to-end logits
parity on SmolLM-135M Q4_K_M still passes within `Tight` tolerance.

### KvUpdate fusion (`af8b2b4 + b354700`) — eager-path +27% over post-graph baseline

Single fused decode-step kernel `fused_rope_kv_write_f16` (and `_dyn` variant
for graph) replaces 3 eager launches per layer (`rope_f16` + 2× cuMemcpyDtoDAsync
into KvCache) with one. Four thread regions in one grid: Q rotation pairs (in
place on `qSrc`), K rotation pairs (rotated value written **directly** to
cache row `kCacheBase + cachePos*kvStride`), K tail copy (no-op when
`ropeDim == headDim`, common), V plain copy (`vSrc → vCacheBase + cachePos*kvStride`).
576 threads = 3 blocks for SmolLM-135M.

Eager (clean) jumped 96 → 118 tok/s (+23%). KvUpdate category collapsed from
**16.1% to 3.9%** of GPU time (2.58 → 0.54 ms/token). Graph path unchanged
(already amortised launch overhead).

Decode-only (gated on `seqLen == 1`) and FP16 cache only (`kvCache is CudaKvCache`,
not the quantized subclass — that one has its own graph-friendly path now).
Prefill keeps the bulk-memcpy fast path.

### Quantized KV cache → graph (`af22540 + 7edb993`) — 3.09× for KV-quant configs

Move `CudaQuantizedKvCache` per-layer state from host (`_layerQuantizedLength[]`,
ring index) to device-resident counters that share the existing `_decodePosDevice`.
New `_dyn` kernel variants in `quant_kv.cu` and `kv_write.cu`:
- `quant_f16_to_q8_0_dyn` / `_q4_0_dyn` — read decode pos from device pointer,
  predicate "evict?" device-side (window not full → no-op early return)
- `kv_write_one_f16_ring` — writes new K/V to ring slot `pos % windowSize`
- `kv_dequant_q8_0_dyn` / `_q4_0_dyn` — dequant `[0, max(0, pos+1−windowSize))` prefix
- `kv_window_to_scratch_dyn` — scatter live FP16 ring rows to scratch positions

Critical ordering invariant in `UpdateDeviceForGraph`: **quantize-on-evict
must run BEFORE ring-write** — otherwise the about-to-be-evicted slot gets
overwritten by the new row before being captured into the Q-cache. Synthetic
test (`UpdateDeviceForGraph_MatchesEager_AcrossEvictionBoundary`) catches this
within the first eviction cycle.

Q8_0 KV + graph: **191.0 tok/s vs 61.8 tok/s eager = 3.09×.** Matches FP16+graph
within noise (5.24 vs 5.28 ms / token), confirming kernel dispatch is no longer
the bottleneck for KV-quant configs either.

### CUDA Graphs decode (the big win, 2.16×)

`c591daf + dd04797 + d941329`. Decode forward is launch-overhead bound on Windows WDDM
(~22 µs/launch × ~400 launches/decode ≈ 9 ms). `cuStreamBeginCapture` → eager forward
→ `cuStreamEndCapture` → `cuGraphInstantiateWithFlags` runs once on the first decode
step after prefill; subsequent steps `cuGraphLaunch` a single stream packet.

**Key trick (option 3 from the design doc):** seq_kv and the RoPE/attention
position offset are **device-resident** (two 4-byte device ints, `_decodePosDevice`
and `_decodeSeqKvDevice`). Host bumps both via `cuMemcpyHtoD_v2` (~1 µs each)
before each replay. Kernel arg pointers bake into the graph at instantiate time;
the values they dereference do not. Topology stays invariant across decode steps.

`attention.cu` body extracted into a `__device__ __forceinline__` helper; two
`extern "C"` entry points: `attention_f16` (scalar seq_kv, prefill / eager decode)
and `attention_f16_dyn` (reads `seq_kv_ptr`, `position_offset_ptr`). New
`native/kernels/kv_write.cu` (`kv_write_one_f16`) replaces the eager
`cuMemcpyDtoDAsync` whose host-computed dst would clobber the same row each
replay — kernel computes `dst = base + posPtr[0] * stride` device-side. PTX
rebuilt for `compute_75` via `build_ptx.bat`.

`UseGraphCapture` flag on `CudaTransformerModel` enables it. Only the standard
`CudaKvCache` path is graph-capable today (see Open §1).

### Fused QKV + Gate/Up GEMV (`c5fedf8 + ce83ce5`)

Eager decode path packs Q/K/V quantized weights into one `[(n_q + 2*n_kv) × hidden]`
matrix at load (`CudaWeights.LoadFromGguf` → `TryPackThree`/`TryPackTwo`,
byte-concat along N via `cuMemcpyDtoDAsync_v2`). Single quantized GEMV per
group; downstream consumers (RoPE, KV update, attention) take pointer slices
into a packed scratch on `CudaForwardState`. Same trick for gate+up.

Works because every GGUF quantized format independently quantizes each row
along K — concatenating along N just stacks rows. Existing
`quantized_gemv_*` PTX kernels work unchanged with bigger N. Memory cost on
SmolLM-135M Q4_K_M: ~9.3 MB Qkv + ~30 MB GateUp = trivial.

**Decode-only.** Prefill (seqLen > 1) keeps per-tensor cuBLAS HGEMM — packing
along N would make rows non-contiguous per-token. Mixed-quant configs cleanly
fall through to 3/2-call path via null-pointer gate.

### Per-category decode profiler (`130ea21`)

`profile-cuda-decode` subcommand on benchmarks runner. Two layers of timing:
total GPU wallclock via cuEvent bracket, plus 12-bucket per-category breakdown
via `MarkProfile(ProfileCategory)` between kernel groups in `Forward`. Walks
event pool after `cuStreamSynchronize` to compute medians. Subcommand modes:
`--graph`, `--compare` (eager + eager-clean + graph side-by-side),
`--no-profiling` (true wall without cuEventRecord overhead). `CudaInferenceBenchmarks`
gets BenchmarkDotNet coverage.

### CUDA 13 cuBLAS resolver fix (`55b2a65`)

CUDA 13 moved cublas64_13.dll, cudart64_13.dll, nvrtc into `bin\x64\` on Windows
and the installer no longer adds that subdir to PATH. `CudaLibraryResolver`
falls back to probing `%CUDA_PATH%`, `%CUDA_HOME%`, and any `CUDA_PATH_V*`
under both new (`bin\x64`) and legacy (`bin`) layouts. Verified on a clean
PATH with no CUDA entries.

## CUDA decode performance (RTX 3060, SmolLM-135M Q4_K_M, 96-prefill / 200-decode)

| Path | tok/s p50 | wall ms p50 | Speedup over baseline |
|---|---|---|---|
| baseline (Q4_K_M, pre-session, FP16 KV) | **87.5** | ~11.4 | 1.00× |
| eager + per-category profiling | 68.9 | 14.52 | (instrumented; not real perf) |
| **eager (clean, FP16 KV)** | **116.7** | 8.57 | **1.33×** |
| **graph (FP16 KV)** | **217.2** | 4.60 | **2.48×** |
| graph (Q8_0 KV — measured before MMQ Q5/Q6) | 191.0 | 5.24 | 2.18× |
| eager (Q8_0 KV — pre-A/B baseline) | 61.8 | 16.18 | (3.09× eager→graph for KV-quant) |

Eager: 1.33× over baseline driven mostly by QKV+Gate/Up fusion and KvUpdate
fusion (those reduce launch count). Graph: 2.48× over baseline; CUDA Graphs
collapses launch overhead, then MMQ-Q4_K (commit `6368950`) lifted graph
188 → 200 tok/s, then MMQ-Q6_K (commit `d0004c2`) lifted **graph 206 → 217 tok/s
via the LmHead** (Q6_K weight). Cumulative arc: 87.5 → 217.2 tok/s = 2.48×.

**WDDM per-launch overhead measured at ~22 µs.** Derived from the wall-time
delta between profiles with and without per-category event marks (270 extra
cuEventRecord calls added 6 ms = ~22 µs each). 400 launches × 22 µs = ~9 ms,
which was nearly all of decode wallclock — directly explaining why graphs win.

### Per-category breakdown — eager path, post-all-fusions (median ms/token)

```
Norm (rmsnorm/fused-add)      2.130    15.6%   ← ~120 launches; mostly already fused
QkvProj                       2.022    14.8%   ← 30 fused GEMVs
MlpDown                       1.874    13.8%
MlpUp (gate+up)               1.851    13.6%   ← 30 fused GEMVs
Attention                     1.537    11.3%
Rope+Bias+QkNorm              1.464    10.7%   ← residual = pre-Q+K-rotation arg setup
OProj                         0.989     7.3%
LmHead                        0.601     4.4%
KvUpdate                      0.535     3.9%   ← was 16.1%; fused into RoPE+KV-write kernel
Swiglu                        0.524     3.8%
Embed                         0.092     0.7%
```

KvUpdate fell from 16.1% → 3.9% as expected. Top remaining categories (Norm,
QkvProj, MlpDown/Up, Attention) are now all roughly evenly weighted around
11–16% — no single dominant target left on the eager path.

Graph path: per-category breakdown unavailable (cuEventRecord between launches
breaks stream capture). For categorisation, run eager.

## Open items (CUDA branch)

### 1. ✅ DONE: KvUpdate fusion (was 16.1% → now 3.9%)

Landed in `af8b2b4 + b354700`. See description above.

### 2. ✅ DONE: CudaQuantizedKvCache → graph compatibility

Landed in `af22540 + 7edb993`. KV-quant + graph now hits 191 tok/s = 3.09× eager.

### 3. ✅ DONE: Norm kernel rewrite (`facbcd7`)

Vectorized half2/float2 loads + warp-shuffle reduction + shared-memory FP32
sum cache. End-to-end contribution at SmolLM scale is sub-noise but lays
groundwork for larger models. See description above.

### 3a. ✅ DONE: MMQ-style Q4_K + Q5_K + Q6_K GEMV (`6368950 + d0004c2`)

dp4a-based fused dequant+matmul for Q4_K, Q5_K, Q6_K decode. Lifted graph
188 → 200 (Q4_K) → 217 (Q6_K LmHead) tok/s on SmolLM-135M Q4_K_M. See
descriptions above.

Validated at k=4096 on Qwen3-8B Q4_K_M: kernels are correct but **MlpUp
(fused gate+up Q4_K GEMV) only achieves ~50 GB/s effective bandwidth out
of the RTX 3060's 360 GB/s peak**. That's the single biggest perf lever
remaining — see §3c.

### 3c. Open: MlpUp / gate+up GEMV bandwidth gap vs llama.cpp

Per-category breakdown on Qwen3-8B (k=4096, 36 layers, **post-MMQ_MAX_CHUNKS fix**):
- MlpUp 40.9% (35.3 ms / token) ← target
- MlpDown 26.7%, QkvProj 11.2%, LmHead 9.2%, OProj 7.2%, Attention 1.8%

llama.cpp CUDA reports ~25–42 tok/s on Qwen3-8B Q4_K_M on RTX 3060;
dotLLM is at 11.5 eager. ~2-3× gap, almost entirely in the GEMV kernels.

Research note `docs/perf/MLPUP_GEMV_GAP.md` ranked hypotheses; H1 (1-row-per-block
MMVQ) was implemented (commit `f9b8294`) but **regresses by 2× without
prerequisite H4** (pre-quantize activation once per forward). Tensor cores
(H2) confirmed not the answer for batch=1 decode (bandwidth-bound, not
compute-bound; llama.cpp itself doesn't use mma at batch=1).

**Open:** implement H4 (single pre-Q8_1 kernel that quantizes x[k] once
per forward, shared across all GEMVs in the layer). Then flip MMVQ-large
default ON. See "Suggested first actions" §1.

### 3b. Norm category — historical context

4 norms per layer × 30 layers = 120 launches. FusedAddRmsNorm already wraps
the residual + rmsnorm pair. Hard to reduce further without fusing into
adjacent GEMVs (would require GEMV+norm fused kernel). Lower ROI per layer
but largest remaining category — worth a look if anyone wants to chase eager
beyond 118 tok/s.

### 3a. Other remaining eager-path categories

QkvProj (14.8%), MlpDown (13.8%), MlpUp (13.6%), Attention (11.3%) are now
all in a similar band. None is a screaming outlier. The next step is probably
**better quantized GEMV kernels** (MMQ-style fused dequant+matmul à la
llama.cpp, using `dp4a` / tensor cores) rather than further launch-count
reduction. That's a multi-week kernel project.

### 4. Rollback + graph invalidation

`CudaKvCache.Rollback` decrements `_currentLength` but does not auto-invalidate
the cached `cuGraphExec`. Currently safe because speculative decoding (the
only Rollback caller) is restricted to greedy and isn't combined with
`UseGraphCapture`. If/when speculative + graph both turn on for the same
generation, add `kvCache.Rollback` as a graph-invalidation trigger.

### 5. Other quantizations on graph path (validation)

`CudaGraphCaptureEquivalenceTest` only covers Q4_K_M / SmolLM. Same code
path should also work for Q8_0, Q5_K, Q6_K — one-liner to parameterize.

### 6. CUDA HF-parity gates on Ryzen box

Inherited from prior session. After `LoadFromSafetensors` (`919c238`), the
4 skipped HF-parity tests should activate against existing reference JSONs in
`tests/DotLLM.Tests.Integration/Models/Loaders/references/`. Different machine.

### 7. ✅ DONE (kernel-level): CUDA MLA Phase A (F32+FP16), Phase B (latent + W_UK absorbed), MoE FFN (F32)

CUDA MLA primitives all in tree: `CudaMlaAttention.Forward` (F32 Phase A
naive), `.ForwardF16` (FP16 sibling, 0.5× cache memory), `.ForwardLatent`
(Phase B, 7.22× cache reduction on V2-Lite via W_UK absorbed attention),
plus `CudaMoeFfn.Forward` (top-k routing + sequential per-expert SwiGLU
+ shared-expert sum, F32). All validated against CPU oracles within
FP16 noise. **What remains:** `CudaTransformerModel.Forward` dispatch glue
(branch on `MlaConfig is not null` → call MLA + MoE) + `CudaWeights`
DeepSeek loader path + tiny-random integration test. ~2-4 days of glue
work. Phase C (hybrid prefill expand + decode absorbed) and quantized
MLA/MoE weight paths are explicit follow-ups. Mamba-3 still not started
on CUDA (separate workstream).

### 8. Open: dispatch glue for MLA + MoE in CudaTransformerModel.Forward

The kernel-level primitives are landed but not yet called from
`CudaTransformerModel.Forward`. End-to-end DeepSeek-V2/V3 forward on
CUDA will work after this commit lands. ~2-4 days. See "Suggested
first actions" §4.

## Cross-model perf observations (RTX 3060)

**Final cross-model perf (post pre-Q8_1 + MMVQ-large, RTX 3060):**

| Model | k | Layers | tok/s eager | tok/s graph | Eager→Graph | vs session start |
|---|---|---|---|---|---|---|
| SmolLM-135M Q4_K_M | 576 | 30 | 109.4 | **225.9** | **2.07×** | 87.5 → 225.9 = **2.58×** |
| Qwen3-4B Q4_K_M | 2560 | 36 | 51.7 | **54.4** | 1.05× | 19.2 → 54.4 = **2.83×** |
| Qwen3-8B Q4_K_M | 4096 | 36 | 32.4 | **33.2** | 1.02× | (didn't load) → 33.2 |

**Pre-Q8_1 + MMVQ-large lift on Qwen-class** (eager): Qwen3-4B **2.69×**
(19.2 → 51.7), Qwen3-8B **2.82×** (11.5 → 32.4). SmolLM unchanged (k=576
< 1024 threshold keeps it on the on-the-fly path).

**Graph capture re-evaluated:** at the new speeds, graph is now **never
slower**, sometimes faster:
- SmolLM (small k, lots of launches): graph **2.07×** — biggest win, as
  expected (launch-overhead-bound regime).
- Qwen3-4B: graph **1.05×** — marginal but positive (was 1.03× pre-pre-Q8_1).
- Qwen3-8B: graph **1.02×** — flipped from 0.96× regression to slight
  win. Pre-Q8_1 reduced wall by 63%, so the fixed launch-overhead
  contribution grew relative to total → graph captures it better.

**Update**: `UseGraphCapture` is now **default-ON** (commit `4edadc4`)
since graph never hurts at any k post-pre-Q8_1. `DOTLLM_DISABLE_GRAPH_CAPTURE=1`
overrides for diagnostic use. The `--compare` benchmark runner explicitly
opts the eager pass into eager so the comparison still works.

**Where dotLLM lands** vs llama.cpp on RTX 3060 (third-party numbers
for context, not run on this box):
- SmolLM-class: not commonly benched at this scale — 225 tok/s is fast.
- 4B-class: llama.cpp typically reports ~50-70 tok/s — dotLLM 54 graph
  is in range.
- 8B-class: llama.cpp typically reports ~25-42 tok/s — dotLLM 33 graph
  is **inside** the range, modestly mid-pack.

The 8B Q4_K_M path is now competitive — we're inside the noise band of
the leading reference impl, on the same hardware.

**The graph→eager crossover is dramatic.** At k=576, decode is launch-overhead
bound (~9 ms / 13 ms of decode wall is launch dispatch) and graphs collapse it
to ~5 ms. At k=2560+ the launches are buried in compute and graph capture's
fixed instantiate cost actually slightly hurts. So `UseGraphCapture` should
remain a per-model knob; the engine should not assume graph is always wins.

**MlpUp (fused gate+up GEMV) dominates at scale** — 38–41% of GPU time on
4B+ models. Per-layer cost on Qwen3-8B: 1.11 ms for a 24576×4096 Q4_K GEMV.
That works out to ~50 GB/s effective memory bandwidth — only ~14% of the
RTX 3060's 360 GB/s peak. The MMQ kernel is correct but not bandwidth-saturating
at large N. This is the single largest perf lever remaining (see Open §3c).

Compared to llama.cpp Vulkan reports on the same RTX 3060 hardware
(~25–40 tok/s for Qwen3-8B Q4_K_M typical), dotLLM is leaving roughly
2–4× on the table on the gate+up kernel specifically.

## Suggested first actions

1. **Force-push** rebased branch — origin is stale at the pre-rebase tip:
   `git push --force-with-lease origin feature/mamba-3-cuda`
   (Local HEAD is `3f2d746` post-rebase; 57 of those commits had their
   SHAs rewritten, plus 4 new commits from Tier 1+2 this round.)
2. `bash scripts/gpu-lock.sh acquire main "GPU test suite" 1200 && CUDA_PATH=... dotnet test tests/DotLLM.Tests.Unit/ --filter "Category=GPU" && bash scripts/gpu-lock.sh release main` — sanity (191/202 GPU tests pass on this box, 11 skip on missing model fixtures).
3. `bash scripts/gpu-lock.sh acquire main "bench" 600 && dotnet run -c Release --project benchmarks/DotLLM.Benchmarks -- profile-cuda-decode --compare && bash scripts/gpu-lock.sh release main` — reproduce 117 eager / 217 graph tok/s on SmolLM. Set `DOTLLM_BENCH_MODEL_PATH=...` to point at Qwen3-4B / Qwen3-8B for k=4096 perf. Should be unchanged post-rebase (parent's CPU/engine churn doesn't touch the GPU hot path).
4. **Recommended next moves (in priority order):**
   - **Read `docs/perf/DEEPSEEK_QUANTIZED_GPU_PATH.md`** — full design note for tasks #9 + #10 with code sketches, alignment risks, and a 5-sub-task ordering. ~14h total focused work, likely 2 sessions.
   - **Task #9 (Tier 3b): Quantized MLA weight paths** — five sub-tasks per the design note: (i) CPU loader skips F32 dequant, stores raw quant bytes; (ii) CUDA loader uploads raw Q4_K + per-projection F16 scratch; (iii) `CudaMlaAttention.ForwardF16` branches on precision (decode → quantized GEMV; prefill → dequant-to-scratch + HGEMM). Decode path matches the existing GQA `Project` helper. The 10.4 GB cached `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf` already lives at `~/.dotllm/models/bartowski/...` so the smoke test is unblocked.
   - **Task #10 (Tier 3c): Quantized MoE + per-expert quantized GEMV** — same pattern for the FFN side. Risk: 192 launches/layer/token at 22 µs WDDM cost = 4.2 ms/layer dispatch overhead. Sequential-per-expert is the v1; grouped-GEMM is a stretch goal that probably becomes an issue when decode tok/s drops below 5.
   - **Task #11: Real DeepSeek-V2-Lite Q4_K_M end-to-end smoke** — gated on #9+#10. Already-downloaded GGUF means the cost is just the test scaffolding. `DebugMaxLayers=2` lets you exercise the new quantized MLA path without hitting MoE first.
   - **MLA Phase C** (hybrid: prefill expand + decode absorbed) — explicit deferral. Phase B alone already gets 7×–14× cache savings; Phase C is a perf refinement worth ~1 week of work post-3b/3c.
   - **CUDA HF-parity gates on Ryzen+iGPU box** — 4 currently skip on this box because safetensors checkpoints aren't local. Different machine.
   - **Continuous batching scheduler** (separate workstream, engine-layer) — prerequisite for the tensor-core mma kernel ROI per `docs/perf/MMA_BATCHED_MMQ.md`. ~6-8 weeks of engine work.
5. **PRs open:** [#13 stacked on feature/mamba-3](https://github.com/jamesburton/dotLLM/pull/13) (this branch — needs description update post-rebase) and [#136 → upstream main](https://github.com/kkokosa/dotLLM/pull/136) (parent).

## Operational gotchas

- **Background VRAM hogs on this box** (RustDesk, Edge WebView, Ollama,
  etc.) can consume ~6 GiB of the RTX 3060's 12 GiB. Qwen3-8B fits with
  cleared background processes; with default Windows session it OOMs even
  after weight dedup. Per-row embedding-lookup fix would close the last
  gap (~1 GiB more).
- **Concurrent GPU agents** were a problem (CUDA_ERROR_ILLEGAL_ADDRESS in
  `cuCtxCreate_v2`); now solved by `scripts/gpu-lock.sh`. Brief sub-agents
  to wrap GPU commands with `acquire <name> <reason> 1200 / release <name>`.
  4 parallel-agent rounds this session; only the first 3 had per-agent
  retry-with-sleep, round 4 used the mutex and was clean.
- **Worktree-leak gotcha**: 6 of the session's sub-agents had Edit/Write
  tools target paths under `E:\Development\dotLLM\` (the parent repo)
  instead of their own worktree. Mitigation in agent briefs: explicitly
  state the worktree absolute path and require `pwd` verification before
  any edit. The recovery pattern (cp + git checkout) is documented in
  the round-4 section.

## DO NOT do early

- Merge `main` into this branch (nothing merge-worthy on main).
- Push without confirming CUDA tests pass on the local box (`feature/mamba-3-cuda` carries CUDA-specific changes; CI without GPU will skip them but you want a sanity local check).
- Delete agent worktrees without confirming commits landed (this session: cleanup is safe — graphs and QKV branches are merged via cherry-pick into `feature/mamba-3-cuda`).
- Regenerate existing reference JSONs — they're the oracle.
- Revert the Llama RoPE fix on parent (`745b091`) — correctness fix.

## Carried forward — parent branch (feature/mamba-3) — see prior session

- 1343+ unit pass / 0 fail / 37 skip on parent.
- HF-reference parity 5/5 argmax on Qwen2.5/TinyLlama/Phi-3.5/DeepSeek-V2-Lite.
- CPU MLA Phase A/B/C, YaRN wired, Vulkan/HIP scaffolded.
- Cross-library perf characterised: dotLLM CPU 38 tok/s decode vs llama.cpp Vulkan iGPU 392 tok/s on same hardware. **CUDA decode now 225.9 tok/s graph (SmolLM-135M Q4_K_M) / 33.2 tok/s graph (Qwen3-8B Q4_K_M) on RTX 3060**, Qwen3-8B inside llama.cpp's reported range on the same hardware.

See git log on `feature/mamba-3` for full detail (commit `e7b4aef` and prior).
