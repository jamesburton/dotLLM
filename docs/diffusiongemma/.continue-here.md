---
phase: diffusiongemma
status: in_progress
last_updated: 2026-06-16
---

<current_state>
Building DiffusionGemma (Google's Gemma-4-26B-A4B-MoE block masked-diffusion model) inference
support in dotLLM, as a stack of branches on the fork (jamesburton/dotLLM) off `origin/dev`.
The full masked-diffusion DECODE engine is implemented and **real-weight validated on LLaDA-8B**
("The capital of France is" → "Paris."). Two threads remain: (#40) the Gemma-4/3n model
architecture for real DiffusionGemma-26B, and (#41) the Vulkan/GPU path for competitive throughput.
Vulkan Gemma backbone parity (#35) is DONE and passing on the real GPU.

Full narrative + environment constraints: docs/diffusiongemma/OVERNIGHT-PROGRESS.md
Plan + proposed issues: docs/diffusiongemma/README.md, PR-PLAN.md, proposed-issues/, VALIDATION.md
</current_state>

<completed_work>
All committed + pushed to fork (branches stack each on the prior, off origin/dev):
- dev-diffusiongemma — planning epic (README/PR-PLAN/VALIDATION/proposed-issues 01–12)
- dg-pr1-gemma-backbone (#23) — Gemma backbone: embed-scale, GeGLU, 4-norm, (1+w), QK-norm
- dg-pr2-gemma4-moe (#24,#25) — Gemma-4 MoE arch, per-attn partial RoPE, dual KV-heads, GeGLU experts, loader
- dg-pr3-bidirectional-attn (#26) — Causal/Bidirectional/Hybrid attention seam (causal byte-identical)
- dg-pr4-diffusion-config (#27) — DiffusionConfig + mask-token resolution
- dg-pr5-denoise-scheduler-sampler (#30,#31) — denoise scheduler + entropy-bound unmask sampler
- dg-pr6-diffusion-text-generator (#28) — DiffusionTextGenerator (canvas denoise loop, multi-canvas, streaming)
- dg-pr7-diffusiongemma-model-loader (#29) — diffusion_gemma loads+generates (synthetic end-to-end)
- dg-pr8-server-diffusion (#34) — server routes diffusion → DiffusionTextGenerator
- dg-pr9-llada-validation (#32) — GGUF→diffusion wiring + llada→Llama mapping + CanvasAttentionMode fix
  + REAL LLaDA-8B validation PASSES ("Paris.")
- dg-pr10-perlayer-headdim (#36) — per-layer head_dim (Gemma-4 dual 256/512), cacheless
- dg-pr12-vulkan-gemma (#41/#35) — Vulkan Gemma backbone parity (embed-scale, GeGLU, 4-norm, softcap/QPAS);
  5/5 CPU↔Vulkan parity tests PASS on the real AMD Radeon 8060S GPU; full Vulkan suite 99/0.

Upstream PRs already open (separate, off upstream/main): #326 Anthropic Messages API, #328 MEAI IChatClient.
Follow-up issues filed on fork: #35 (done via dg-pr12), #36 (done), #37 (KV-prefix reuse), #40, #41.
</completed_work>

<remaining_work>
- **#40 (Gemma-4/3n + GGUF) — the deep one.** WIP on branch `dg-pr11-gemma4-arch` (commit bd7a60c):
  ONLY partial GGUF gemma4/diffusion-gemma arch mapping in GgufModelConfigExtractor — UNVERIFIED, does
  NOT build/run yet. The hard, unstarted part: implement Gemma-4/3n **per-layer embeddings (PLE)**
  (GGUF key `gemma4.embedding_length_per_layer_input`) + likely **AltUp/Laurel** in the backbone, then
  finish GGUF gemma4/diffusion-gemma config extraction, then validate against the LOCAL real
  `gemma-4-26B-A4B-it-UD-Q4_K_M.gguf` (AR, no diffusion) — a single 26B Q4 CPU forward (~1-3 min) should
  yield a sensible next token (e.g. " Paris"). Then the diffusion-gemma GGUF run.
- **#41 item 2 — Vulkan non-causal attention.** NOT started. The Vulkan attention kernel is causal-only;
  a bidirectional/hybrid GPU attention kernel is the enabler for GPU diffusion throughput. + #33 throughput
  bench + #37 KV-prefix reuse.
</remaining_work>

<decisions_made>
- Validate on LLaDA-8B first (Llama backbone, runs on existing support + our diffusion decode) — supersedes
  diffugpt-s (GPT-2, unsupported). Real validation proven there.
- Mask mode is model-specific: LLaDA = fully Bidirectional; DiffusionGemma = Hybrid (block-AR). Selected via
  DiffusionConfig.CanvasAttentionMode (a diagnostic showed Hybrid→all-EOS, Bidirectional→"Paris" on LLaDA).
- Use small GGUF quants (disk is the bottleneck — only ~20GB free on C:). Freed 18.7GB by deleting unused
  RyzenAI NPU/ONNX models (re-downloadable; left the user's Qwen3.6 models alone).
- Vulkan (not ROCm) is the GPU path (no rocminfo; AMD Radeon 8060S gfx1151, Vulkan + glslc present).
- #36: per-layer head_dim done on the CACHELESS path (diffusion is cacheless); KV-cached AR path guarded.
- Per-PR stacked branches, each self-verified (build 0/0 + CPU tests) before commit.
</decisions_made>

<blockers>
- DiffusionGemma-26B correctness needs Gemma-4/3n PLE/AltUp (not implemented) — that's #40, a real arch effort.
- Throughput: 8B/26B on CPU is impractical (~16.7 min/LLaDA gen, recompute-per-step) → needs GPU diffusion
  (#41 item 2) + KV-prefix reuse (#37).
- Disk: ~20GB free on C: (now ~33GB after cleanup). Big safetensors won't fit; use GGUF quants.
- Agent A (#40) timed out (API stream idle) mid-run — needs a fresh focused session.
</blockers>

<context>
Local model assets on disk (no re-download needed): C:\models\llada\LLaDA-8B-Instruct.Q4_K_M.gguf (4.93G),
C:\models\llada\diffusiongemma-26B-A4B-it-Q4_K_M.gguf (16.81G), and the HF cache
models--unsloth--gemma-4-26B-A4B-it-GGUF\...\gemma-4-26B-A4B-it-UD-Q4_K_M.gguf (15.78G, AR backbone).
GGUF arch strings: "llada"→Llama (done), "gemma4" / "diffusion-gemma" (to map in #40). The diffusion path
is cacheless (PR-3 guards non-causal+KV-cache). Worktrees: dev-diffusiongemma (this, branch dg-pr10),
dg-gemma4-arch (dg-pr11 WIP), dg-vulkan (dg-pr12). Gated real-weight tests use env vars
DOTLLM_LLADA_GGUF / DOTLLM_GEMMA4_GGUF, SkippableFact, under tests/DotLLM.Tests.Integration/.
</context>

<next_action>
Resume #40 in a FRESH session (Gemma-3n architecture is deep): on branch dg-pr11-gemma4-arch,
(1) research HF transformers modeling_gemma3n.py for PLE/AltUp/Laurel, (2) implement PLE (per-layer
embedding lookup added to the hidden state each layer, gated to Gemma-4), (3) add GGUF gemma4/
diffusion-gemma config extraction, (4) validate with one forward of the local gemma-4-26B GGUF
(expect a sensible next token). Keep #41 item 2 (Vulkan non-causal attention) as a separate parallel track.
</next_action>
